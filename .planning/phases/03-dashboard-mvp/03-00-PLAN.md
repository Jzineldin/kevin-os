---
phase: 03-dashboard-mvp
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/postcss.config.mjs
  - apps/dashboard/src/app/globals.css
  - apps/dashboard/src/app/layout.tsx
  - apps/dashboard/src/app/page.tsx
  - apps/dashboard/vitest.config.ts
  - apps/dashboard/playwright.config.ts
  - apps/dashboard/lighthouserc.json
  - apps/dashboard/tests/unit/setup.ts
  - apps/dashboard/tests/e2e/fixtures.ts
  - apps/dashboard/tests/e2e/pwa-install.spec.ts
  - apps/dashboard/tests/e2e/pwa-offline.spec.ts
  - apps/dashboard/tests/e2e/sse-reconnect.spec.ts
  - apps/dashboard/tests/e2e/merge-audit.spec.ts
  - apps/dashboard/tests/e2e/inbox-keyboard.spec.ts
  - apps/dashboard/tests/e2e/auth-middleware.spec.ts
  - apps/dashboard/tests/e2e/today.spec.ts
  - apps/dashboard/tests/e2e/entity.spec.ts
  - apps/dashboard/tests/e2e/timeline.spec.ts
  - apps/dashboard/tests/e2e/calendar.spec.ts
  - apps/dashboard/tests/integration/api-layer.test.ts
  - apps/dashboard/tests/integration/sse.test.ts
  - services/dashboard-api/package.json
  - services/dashboard-api/tests/merge-transactional.test.ts
  - services/dashboard-api/tests/merge-partial.test.ts
  - services/dashboard-api/tests/merge-resume.test.ts
  - services/dashboard-api/tests/timeline.test.ts
  - services/dashboard-listen-relay/package.json
  - services/dashboard-listen-relay/tests/listen-reconnect.test.ts
  - services/dashboard-notify/package.json
  - services/dashboard-notify/tests/notify-payload.test.ts
  - packages/contracts/src/dashboard.ts
  - packages/contracts/src/index.ts
  - packages/test-fixtures/src/dashboard/index.ts
  - .github/workflows/dashboard-ci.yml
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, ENT-07, ENT-08, INF-12]
requirements_addressed: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, ENT-07, ENT-08, INF-12]

must_haves:
  truths:
    - "apps/dashboard workspace exists and typechecks"
    - "vitest + Playwright + Lighthouse CI commands run (stubs fail/pending allowed)"
    - "packages/contracts/src/dashboard.ts exports all zod schemas consumed by Vercel and dashboard-api"
    - "18 Wave-0 gap files from 03-VALIDATION.md exist as stubs"
  artifacts:
    - path: "pnpm-workspace.yaml"
      contains: "apps/*"
    - path: "apps/dashboard/package.json"
      contains: "@kos/dashboard"
    - path: "packages/contracts/src/dashboard.ts"
      exports: ["TodayResponseSchema", "EntityResponseSchema", "TimelineRowSchema", "InboxItemSchema", "InboxApproveSchema", "MergeRequestSchema", "CapturePostSchema", "SseEventSchema"]
    - path: "apps/dashboard/vitest.config.ts"
      provides: "Vitest 2.x jsdom config"
    - path: "apps/dashboard/playwright.config.ts"
      provides: "Playwright Chromium + mobile Android projects with baseURL env"
    - path: "apps/dashboard/lighthouserc.json"
      provides: "Lighthouse CI perf budgets (TTI < 1.5s Today, TBT < 300ms, CLS < 0.1)"
    - path: ".github/workflows/dashboard-ci.yml"
      provides: "Quick suite on PR + full suite on main"
  key_links:
    - from: "apps/dashboard/src/app/layout.tsx"
      to: "packages/contracts/src/dashboard.ts"
      via: "import type { TodayResponse } from '@kos/contracts/dashboard'"
      pattern: "from ['\"]@kos/contracts/dashboard['\"]"
    - from: "services/dashboard-api"
      to: "packages/contracts/src/dashboard.ts"
      via: "shared zod schemas for request/response validation"
      pattern: "from ['\"]@kos/contracts/dashboard['\"]"
---

<objective>
Scaffold the Phase 3 monorepo surface: `apps/dashboard` workspace (Next 15.5.x + React 19.2.5 + Tailwind 4.2.4), three new `services/dashboard-*` workspaces, shared zod contracts, and all 18 Wave-0 test-infra gaps from 03-VALIDATION.md. No feature code — this plan exists so every Wave-1+ plan has a typed contract, a test file, and a working pnpm workspace to extend.

Purpose: Nyquist-compliant test-harness-first per 03-VALIDATION.md. Every later task in Phase 3 references files created here.
Output: Running `pnpm --filter @kos/dashboard test` returns (0 passed or expected failures) in <60s; `pnpm --filter @kos/dashboard typecheck` passes; 18 stub files exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-mvp/03-CONTEXT.md
@.planning/phases/03-dashboard-mvp/03-VALIDATION.md
@.planning/phases/03-dashboard-mvp/03-UI-SPEC.md
@.planning/phases/03-dashboard-mvp/03-RESEARCH.md
@pnpm-workspace.yaml
@packages/contracts/src/index.ts
@tsconfig.base.json
@CLAUDE.md

<interfaces>
<!-- Existing monorepo shape — DO NOT CHANGE -->
From pnpm-workspace.yaml (current):
```yaml
packages:
  - 'packages/*'
  - 'services/*'
```
Add: `- 'apps/*'` — per RESEARCH §1.

From packages/contracts/src/index.ts — existing barrel exports (Phase 2 contracts). Extend, do not replace.

From tsconfig.base.json — `target: ES2022`, `module: ESNext`, `moduleResolution: Bundler`, `strict: true`. New `apps/dashboard/tsconfig.json` MUST extend this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add apps/* to pnpm-workspace + scaffold apps/dashboard Next 15.5.x app + Tailwind v4 theme port from TFOS-ui.html :root</name>
  <read_first>
    - pnpm-workspace.yaml (read full file to confirm current globs)
    - tsconfig.base.json (extend, do not duplicate)
    - TFOS-ui.html lines 12-100 (the `:root` design-token block — source of truth for D-07, D-08)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §1 (monorepo bootstrap steps 1-7)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §2 (Tailwind v4 @theme port, full CSS block lines 264-363)
    - .planning/phases/03-dashboard-mvp/03-UI-SPEC.md §Design System + §Color Tokens + §Typography + §Spacing Scale + §Radii + §Motion Contract
    - CLAUDE.md §"Recommended Stack" + §"What NOT to Use"
  </read_first>
  <behavior>
    - Given the monorepo at repo root, `pnpm install` resolves apps/dashboard as workspace `@kos/dashboard`.
    - Given `apps/dashboard/src/app/page.tsx` renders `<div data-token-test>{getComputedStyle hex}</div>`, the Vitest+jsdom check reads `--color-bg` as `#0a0c11`, `--color-accent` as `#7c5bff`, `--color-surface-1` as `#11141b`.
    - Given Geist fonts are loaded via `next/font/google`, the root layout class list contains both `--font-geist-sans` and `--font-geist-mono` CSS variables.
    - `pnpm --filter @kos/dashboard typecheck` exits 0 (TS strict mode, extends tsconfig.base.json).
  </behavior>
  <action>
    1. **Edit `pnpm-workspace.yaml`** — add `- 'apps/*'` under `packages:` list. Preserve existing entries verbatim.

    2. **Create `apps/dashboard/package.json`** with these exact keys:
       ```json
       {
         "name": "@kos/dashboard",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "engines": { "node": ">=22.12.0" },
         "scripts": {
           "dev": "next dev",
           "build": "next build",
           "start": "next start",
           "typecheck": "tsc --noEmit",
           "lint": "next lint",
           "test": "vitest run",
           "test:watch": "vitest",
           "e2e": "playwright test",
           "lhci": "lhci autorun"
         }
       }
       ```
       Then run these installs (DO NOT use `create-next-app` per RESEARCH §1 step 3):
       ```bash
       pnpm --filter @kos/dashboard add next@15.5.4 react@19.2.5 react-dom@19.2.5
       pnpm --filter @kos/dashboard add -D typescript@5.7.2 @types/node @types/react@19 @types/react-dom@19
       pnpm --filter @kos/dashboard add -D tailwindcss@4.2.4 @tailwindcss/postcss@4.2.4 postcss
       pnpm --filter @kos/dashboard add "@kos/contracts@workspace:*" "@kos/db@workspace:*"
       ```

    3. **Create `apps/dashboard/tsconfig.json`**:
       ```json
       {
         "extends": "../../tsconfig.base.json",
         "compilerOptions": {
           "jsx": "preserve",
           "plugins": [{ "name": "next" }],
           "paths": { "@/*": ["./src/*"] },
           "allowJs": true,
           "incremental": true,
           "types": ["node", "vitest/globals"]
         },
         "include": ["next-env.d.ts", "src/**/*", "tests/**/*", ".next/types/**/*.ts"],
         "exclude": ["node_modules"]
       }
       ```

    4. **Create `apps/dashboard/next.config.ts`** per RESEARCH §1 step 5 + P-14 (Next 15 only):
       ```ts
       import type { NextConfig } from 'next';
       const config: NextConfig = {
         reactStrictMode: true,
         transpilePackages: ['@kos/db', '@kos/contracts'],
         experimental: { typedRoutes: true },
       };
       export default config;
       ```

    5. **Create `apps/dashboard/postcss.config.mjs`**:
       ```js
       export default { plugins: { '@tailwindcss/postcss': {} } };
       ```

    6. **Create `apps/dashboard/src/app/globals.css`** — paste the EXACT @theme block from RESEARCH §2 lines 266-363 verbatim. This includes:
       - Surfaces: `--color-bg: #0a0c11`, `--color-surface-1: #11141b`, `--color-surface-2: #161a23`, `--color-surface-3: #1c2029`, `--color-surface-hover: #1f2430`
       - Borders: `--color-border: #232732`, `--color-border-hover: #2d3340`, `--color-border-strong: #383e4d`
       - Text: `--color-text: #e7eaf0`, `--color-text-2: #b1b6c2`, `--color-text-3: #71768a`, `--color-text-4: #4d5263`
       - Accent: `--color-accent: #7c5bff`, `--color-accent-2: #9d80ff`
       - Status: `--color-success: #34d399`, `--color-warning: #fbbf24`, `--color-danger: #f87171`, `--color-info: #38bdf8`, `--color-pink: #f472b6`
       - Bolag: `--color-tale-forge: #38bdf8`, `--color-outbehaving: #fb923c`, `--color-personal: #a78bfa`
       - Motion: `--ease: cubic-bezier(0.16, 1, 0.3, 1)`, `--ease-out: cubic-bezier(0.22, 1, 0.36, 1)`, `--transition-fast: 120ms`, `--transition-base: 180ms`, `--transition-slow: 280ms`
       - Type: `--text-xs: 11px`, `--text-sm: 12px`, `--text-base: 13px`, `--text-md: 14px`, `--text-lg: 16px`, `--text-xl: 20px`, `--text-2xl: 28px`, `--text-3xl: 36px`
       - Radii: `--radius-sm: 4px`, `--radius-md: 6px`, `--radius-lg: 8px`, `--radius-xl: 12px`, `--radius-2xl: 16px`
       - Fonts: `--font-sans: var(--font-geist-sans)`, `--font-mono: var(--font-geist-mono)`
       - `:root` alpha tokens: `--accent-bg: rgba(124, 91, 255, 0.10)`, `--accent-border: rgba(124, 91, 255, 0.25)`
       - `html, body { background: var(--color-bg); color: var(--color-text); font-family: var(--font-sans); font-size: var(--text-base); line-height: 1.5; font-feature-settings: "cv11", "ss01", "ss03"; -webkit-font-smoothing: antialiased; text-rendering: optimizeLegibility; }`
       - `.mono { font-family: var(--font-mono); }`, `::selection { background: var(--accent-bg); color: var(--color-accent-2); }`
       - `@keyframes pulse-dot { 0%,100% { opacity: 0.4; transform: scale(1); } 50% { opacity: 1; transform: scale(1.12); } }`
       - `.pulse-dot { width: 6px; height: 6px; border-radius: 999px; background: var(--color-accent); animation: pulse-dot 1.4s var(--ease) infinite; }`

    7. **Create `apps/dashboard/src/app/layout.tsx`** per RESEARCH §2 lines 368-382 with Geist + Geist_Mono from `next/font/google` (subsets: ['latin'], display: 'swap'). Include `<html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>`. `export const metadata = { title: 'Kevin OS', description: 'Personal operating system' }`.

    8. **Create `apps/dashboard/src/app/page.tsx`** — a minimal RSC that renders `<main className="pulse-dot" aria-label="Kevin OS" />` so tests can assert tokens resolve.

    9. **Create `apps/dashboard/next-env.d.ts`** (standard Next reference file): `/// <reference types="next" />\n/// <reference types="next/image-types/global" />`.

    10. **Do NOT run shadcn init yet** — that happens in Plan 01. This task only provides tokens.
  </action>
  <verify>
    <automated>pnpm install --lockfile-only && pnpm --filter @kos/dashboard typecheck && pnpm --filter @kos/dashboard build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "'apps/*'" pnpm-workspace.yaml` matches.
    - `grep -E '"next": "\^?15\.5\.' apps/dashboard/package.json` matches (Next 15.5.x pin, NOT 16).
    - `grep -E '"tailwindcss": "\^?4\.2\.4"' apps/dashboard/package.json` matches (Tailwind 4.2.4 pin).
    - `grep -E '"react": "\^?19\.2\.5"' apps/dashboard/package.json` matches.
    - `grep -F "@kos/contracts" apps/dashboard/package.json` matches.
    - `grep -c -F "#0a0c11" apps/dashboard/src/app/globals.css` ≥ 1 (bg token present).
    - `grep -c -F "#7c5bff" apps/dashboard/src/app/globals.css` ≥ 1 (accent token present).
    - `grep -F "next/font/google" apps/dashboard/src/app/layout.tsx` matches.
    - `grep -F "Geist_Mono" apps/dashboard/src/app/layout.tsx` matches.
    - `test -f apps/dashboard/next.config.ts && grep -F "transpilePackages" apps/dashboard/next.config.ts` matches.
    - `pnpm --filter @kos/dashboard typecheck` exits 0.
    - `pnpm --filter @kos/dashboard build` completes without Tailwind plugin errors.
  </acceptance_criteria>
  <done>
    Monorepo recognises @kos/dashboard; tokens from TFOS-ui.html `:root` are present 1:1 in globals.css; Next 15 + Tailwind 4 + Geist fonts build successfully.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author packages/contracts/src/dashboard.ts — shared zod schemas for every dashboard-api route</name>
  <read_first>
    - packages/contracts/src/index.ts (current barrel; preserve exports)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §7 (routing table + zod boundary, lines 762-827)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §9 (Today response shape, lines 817-823)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §10 (timeline query + cursor format, lines 927-948)
    - .planning/phases/03-dashboard-mvp/03-CONTEXT.md D-25 (Postgres channel payload contract)
    - packages/db/src/schema.ts (reference shapes for entity_index, mention_events, agent_runs)
  </read_first>
  <behavior>
    - Every dashboard-api route in RESEARCH §7 routing table has a corresponding schema export here.
    - SseEventSchema validates: `{ kind: 'inbox_item'|'entity_merge'|'capture_ack'|'draft_ready'|'timeline_event', id: string, entity_id?: string, ts: iso8601 }` (D-25 verbatim).
    - MergeRequestSchema requires `target_id: string (ULID)`, `source_id: string (ULID)`, `merge_id: string (ULID)`, `diff: Record<string, unknown>`.
    - `z.parse()` on a valid fixture round-trips; on an invalid fixture throws `ZodError`.
    - Re-exported from `packages/contracts/src/index.ts`.
  </behavior>
  <action>
    1. **Create `packages/contracts/src/dashboard.ts`** with these exports (use `zod` v4 syntax — the monorepo pins zod 4.3.6 per RESEARCH Stack Versions):

    ```ts
    import { z } from 'zod';

    // Shared primitives
    export const UlidSchema = z.string().length(26).regex(/^[0-9A-HJKMNP-TV-Z]+$/);
    export const UuidSchema = z.string().uuid();
    export const BolagSchema = z.enum(['tale-forge','outbehaving','personal']);
    export const IsoDateTimeSchema = z.string().datetime();

    // Today view (GET /today) — RESEARCH §9
    export const TodayBriefSchema = z.object({
      body: z.string(),
      generated_at: IsoDateTimeSchema,
    });
    export const TodayPrioritySchema = z.object({
      id: z.string(),
      title: z.string(),
      bolag: BolagSchema.nullable(),
      entity_id: UuidSchema.nullable(),
      entity_name: z.string().nullable(),
    });
    export const TodayDraftSchema = z.object({
      id: z.string(),
      entity: z.string(),
      preview: z.string(),
      from: z.string().nullable(),
      subject: z.string().nullable(),
      received_at: IsoDateTimeSchema,
    });
    export const TodayDroppedThreadSchema = z.object({
      id: z.string(),
      entity_id: UuidSchema,
      entity: z.string(),
      age_days: z.number(),
      bolag: BolagSchema.nullable(),
    });
    export const TodayMeetingSchema = z.object({
      id: z.string(),
      title: z.string(),
      start_at: IsoDateTimeSchema,
      end_at: IsoDateTimeSchema,
      is_now: z.boolean(),
      bolag: BolagSchema.nullable(),
    });
    export const TodayResponseSchema = z.object({
      brief: TodayBriefSchema.nullable(),
      priorities: z.array(TodayPrioritySchema),
      drafts: z.array(TodayDraftSchema),
      dropped: z.array(TodayDroppedThreadSchema),
      meetings: z.array(TodayMeetingSchema),
    });
    export type TodayResponse = z.infer<typeof TodayResponseSchema>;

    // Entity + timeline (GET /entities/:id, GET /entities/:id/timeline)
    export const EntityTypeSchema = z.enum(['Person','Project','Company','Document']);
    export const EntityResponseSchema = z.object({
      id: UuidSchema,
      name: z.string(),
      type: EntityTypeSchema,
      aliases: z.array(z.string()),
      org: z.string().nullable(),
      role: z.string().nullable(),
      relationship: z.string().nullable(),
      status: z.string(),
      seed_context: z.string().nullable(),
      manual_notes: z.string().nullable(),
      last_touch: IsoDateTimeSchema.nullable(),
      confidence: z.number().nullable(),
      linked_projects: z.array(z.object({ id: UuidSchema, name: z.string(), bolag: BolagSchema.nullable() })),
      stats: z.object({
        first_contact: IsoDateTimeSchema.nullable(),
        total_mentions: z.number(),
        active_threads: z.number(),
      }),
      ai_block: z.object({
        body: z.string(),
        cached_at: IsoDateTimeSchema.nullable(),
      }).nullable(),
    });
    export type EntityResponse = z.infer<typeof EntityResponseSchema>;

    export const TimelineRowKindSchema = z.enum(['mention','agent_run','email','transcript','doc','task','decision','merge']);
    export const TimelineRowSchema = z.object({
      id: z.string(),
      kind: TimelineRowKindSchema,
      occurred_at: IsoDateTimeSchema,
      source: z.string(),
      context: z.string(),
      capture_id: z.string().nullable(),
      href: z.string().nullable(),
    });
    export const TimelinePageSchema = z.object({
      rows: z.array(TimelineRowSchema),
      next_cursor: z.string().nullable(), // base64("${iso}:${id}") per §10
    });

    // Inbox (GET /inbox, POST /inbox/:id/...)
    export const InboxItemKindSchema = z.enum(['draft_reply','entity_routing','new_entity','merge_resume']);
    export const InboxItemSchema = z.object({
      id: z.string(),
      kind: InboxItemKindSchema,
      title: z.string(),
      preview: z.string(),
      bolag: BolagSchema.nullable(),
      entity_id: UuidSchema.nullable(),
      merge_id: UlidSchema.nullable(),
      payload: z.record(z.string(), z.unknown()),
      created_at: IsoDateTimeSchema,
    });
    export const InboxListSchema = z.object({ items: z.array(InboxItemSchema) });
    export const InboxApproveSchema = z.object({ edits: z.record(z.string(), z.unknown()).optional() });
    export const InboxEditSchema = z.object({ fields: z.record(z.string(), z.unknown()) });
    export const InboxActionResponseSchema = z.object({ ok: z.literal(true) });

    // Merge (POST /entities/:target_id/merge, .../resume)
    export const MergeRequestSchema = z.object({
      source_id: UuidSchema,
      merge_id: UlidSchema,
      diff: z.record(z.string(), z.unknown()),
    });
    export const MergeResponseSchema = z.object({
      ok: z.boolean(),
      merge_id: UlidSchema,
      resumable: z.boolean().optional(),
    });
    export const MergeResumeRequestSchema = z.object({ merge_id: UlidSchema });

    // Capture (POST /capture)
    export const CapturePostSchema = z.object({
      text: z.string().max(10000).optional(),
      audio_s3: z.string().url().optional(),
    }).refine(v => v.text || v.audio_s3, { message: 'text or audio_s3 required' });
    export const CaptureResponseSchema = z.object({ capture_id: UlidSchema, received_at: IsoDateTimeSchema });

    // SSE event contract (D-25 verbatim)
    export const SseEventKindSchema = z.enum(['inbox_item','entity_merge','capture_ack','draft_ready','timeline_event']);
    export const SseEventSchema = z.object({
      kind: SseEventKindSchema,
      id: z.string(),
      entity_id: UuidSchema.optional(),
      ts: IsoDateTimeSchema,
    });
    export type SseEvent = z.infer<typeof SseEventSchema>;

    // Auth
    export const LoginRequestSchema = z.object({ token: z.string().min(1) });
    export const LoginResponseSchema = z.object({ ok: z.literal(true) });
    ```

    2. **Add to `packages/contracts/src/index.ts`**: `export * from './dashboard.js';` (preserve existing exports). If current file uses `.ts` extensions in its barrel, match whatever style is already present.

    3. **Create `packages/test-fixtures/src/dashboard/index.ts`** — valid-fixture factories that the Wave-1+ tests consume:
       - `makeTodayResponse()`, `makeEntityResponse()`, `makeTimelinePage()`, `makeInboxItem(kind)`, `makeMergeRequest()`, `makeSseEvent(kind)`. Each returns `z.parse`-safe data.
  </action>
  <verify>
    <automated>pnpm --filter @kos/contracts typecheck && node -e "const c=require('./packages/contracts/dist/index.cjs'); console.log(Object.keys(c).filter(k=>/Schema$/.test(k)).length)" 2>/dev/null || pnpm --filter @kos/contracts exec tsc --noEmit src/dashboard.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "export const TodayResponseSchema" packages/contracts/src/dashboard.ts` matches.
    - `grep -F "export const SseEventSchema" packages/contracts/src/dashboard.ts` matches.
    - `grep -F "export const MergeRequestSchema" packages/contracts/src/dashboard.ts` matches.
    - `grep -F "export const InboxItemSchema" packages/contracts/src/dashboard.ts` matches.
    - `grep -F "export const CapturePostSchema" packages/contracts/src/dashboard.ts` matches.
    - `grep -F "export * from './dashboard" packages/contracts/src/index.ts` matches (extension flexibility).
    - `grep -E "inbox_item|entity_merge|capture_ack|draft_ready|timeline_event" packages/contracts/src/dashboard.ts | wc -l` ≥ 5 (all five SSE kinds from D-25 declared).
    - `pnpm --filter @kos/contracts typecheck` exits 0.
    - `pnpm --filter @kos/test-fixtures typecheck` exits 0 (fixtures compile against schemas).
  </acceptance_criteria>
  <done>
    `packages/contracts/src/dashboard.ts` exports every schema listed in must_haves artifacts. Both Vercel (apps/dashboard) and AWS (services/dashboard-api) will import from the same source of truth.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold 3 dashboard services + 18 Wave-0 test stubs + CI workflow (all from 03-VALIDATION.md gap list)</name>
  <read_first>
    - .planning/phases/03-dashboard-mvp/03-VALIDATION.md §"Wave 0 Requirements" (the 18-item list)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §15 (Validation Architecture, tests mapped to requirements)
    - services/notion-indexer/package.json (pattern reuse for Lambda service package.json)
    - .planning/phases/03-dashboard-mvp/03-RESEARCH.md §13 (Fargate task spec for dashboard-listen-relay)
    - packages/test-fixtures/src/ (existing fixtures pattern from Phase 2)
  </read_first>
  <behavior>
    - `pnpm --filter @kos/dashboard-api typecheck` passes with empty handler.
    - `pnpm --filter @kos/dashboard-listen-relay typecheck` passes.
    - `pnpm --filter @kos/dashboard-notify typecheck` passes.
    - `pnpm --filter @kos/dashboard test --run` exits 0 (stubs use `it.todo()` OR `describe.skip` — no failures on bare scaffold).
    - `pnpm --filter @kos/dashboard e2e -- --list` lists every Playwright spec without errors.
    - GitHub Actions workflow parses valid YAML.
  </behavior>
  <action>
    1. **Create 3 service scaffolds** (pattern from `services/notion-indexer/`):

       a. `services/dashboard-api/package.json`:
       ```json
       {
         "name": "@kos/dashboard-api",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "scripts": {
           "typecheck": "tsc --noEmit",
           "test": "vitest run"
         },
         "dependencies": {
           "@kos/contracts": "workspace:*",
           "@kos/db": "workspace:*",
           "@notionhq/client": "2.3.0",
           "drizzle-orm": "0.36.0",
           "pg": "8.13.1",
           "@aws-sdk/rds-signer": "3.691.0",
           "ulid": "3.0.2",
           "zod": "4.3.6"
         },
         "devDependencies": {
           "typescript": "5.7.2",
           "vitest": "2.1.4",
           "@types/aws-lambda": "8.10.145",
           "@types/pg": "8.11.10"
         }
       }
       ```
       Create `services/dashboard-api/tsconfig.json` extending `../../tsconfig.base.json`. Create `services/dashboard-api/src/index.ts` with a stub `export const handler = async () => ({ statusCode: 501, body: 'not implemented' });`. Create `services/dashboard-api/src/handlers/.gitkeep`.

       b. `services/dashboard-listen-relay/package.json` (Fastify + pg-listen per RESEARCH §13):
       ```json
       {
         "name": "@kos/dashboard-listen-relay",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run", "start": "node dist/index.js" },
         "dependencies": {
           "@kos/contracts": "workspace:*",
           "fastify": "5.1.0",
           "pg-listen": "1.7.0",
           "pg": "8.13.1",
           "@aws-sdk/rds-signer": "3.691.0"
         },
         "devDependencies": { "typescript": "5.7.2", "vitest": "2.1.4" }
       }
       ```
       Create `services/dashboard-listen-relay/src/index.ts` with a Fastify `/healthz` stub returning `{ ok: true, buffered: 0 }`. `tsconfig.json` extends base.

       c. `services/dashboard-notify/package.json` (tiny EventBridge-rule target Lambda per D-22):
       ```json
       {
         "name": "@kos/dashboard-notify",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
         "dependencies": {
           "@kos/contracts": "workspace:*",
           "pg": "8.13.1",
           "@aws-sdk/rds-signer": "3.691.0"
         },
         "devDependencies": { "typescript": "5.7.2", "vitest": "2.1.4", "@types/aws-lambda": "8.10.145" }
       }
       ```
       Stub `src/index.ts` with `export const handler = async (event: any) => ({ received: event['detail-type'] });`.

    2. **Create `apps/dashboard/vitest.config.ts`** (Vitest 2.x jsdom):
       ```ts
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import path from 'node:path';
       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./tests/unit/setup.ts'],
           include: ['tests/**/*.test.{ts,tsx}'],
           exclude: ['tests/e2e/**', 'node_modules/**'],
         },
         resolve: { alias: { '@': path.resolve(import.meta.dirname, './src') } },
       });
       ```
       Install dev deps: `pnpm --filter @kos/dashboard add -D vitest@2.1.4 @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom`.

    3. **Create `apps/dashboard/tests/unit/setup.ts`**: `import '@testing-library/jest-dom';`.

    4. **Create `apps/dashboard/playwright.config.ts`** per RESEARCH §15:
       ```ts
       import { defineConfig, devices } from '@playwright/test';
       export default defineConfig({
         testDir: './tests/e2e',
         timeout: 30_000,
         use: {
           baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000',
           storageState: process.env.PLAYWRIGHT_STORAGE_STATE,
           trace: 'on-first-retry',
         },
         projects: [
           { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
           { name: 'mobile-android', use: { ...devices['Pixel 7'] } },
         ],
         reporter: [['list']],
         retries: process.env.CI ? 2 : 0,
       });
       ```
       Install: `pnpm --filter @kos/dashboard add -D @playwright/test@1.49.0`. Run `npx playwright install --with-deps chromium` in the CI workflow (not here — keep repo lean).

    5. **Create `apps/dashboard/lighthouserc.json`** with budgets from 03-VALIDATION.md Wave 0 list:
       ```json
       {
         "ci": {
           "collect": {
             "url": ["http://localhost:3000/", "http://localhost:3000/inbox", "http://localhost:3000/calendar"],
             "numberOfRuns": 3,
             "startServerCommand": "pnpm --filter @kos/dashboard start",
             "startServerReadyPattern": "ready on"
           },
           "assert": {
             "assertions": {
               "categories:performance": ["error", { "minScore": 0.9 }],
               "categories:accessibility": ["error", { "minScore": 0.95 }],
               "interactive": ["error", { "maxNumericValue": 1500 }],
               "total-blocking-time": ["error", { "maxNumericValue": 300 }],
               "cumulative-layout-shift": ["error", { "maxNumericValue": 0.1 }]
             }
           },
           "upload": { "target": "temporary-public-storage" }
         }
       }
       ```
       Install at repo root: `pnpm add -D -w @lhci/cli@0.14.0`.

    6. **Create `apps/dashboard/tests/e2e/fixtures.ts`** with `authCookie()` helper that POSTs to `/api/auth/login` with `process.env.KOS_TEST_BEARER_TOKEN` and returns `storageState` (per P-10 in RESEARCH §17).

    7. **Create these 11 Playwright + 1 integration test stubs** (each is a `test.describe('…', () => { test.fixme('scaffolded in Wave 0 — wired in later plans', async () => {}); });` placeholder):
       - `apps/dashboard/tests/e2e/pwa-install.spec.ts` (maps to UI-05 per VALIDATION)
       - `apps/dashboard/tests/e2e/pwa-offline.spec.ts` (UI-05)
       - `apps/dashboard/tests/e2e/sse-reconnect.spec.ts` (UI-06)
       - `apps/dashboard/tests/e2e/merge-audit.spec.ts` (ENT-07)
       - `apps/dashboard/tests/e2e/inbox-keyboard.spec.ts` (UI-04)
       - `apps/dashboard/tests/e2e/auth-middleware.spec.ts` (INF-12)
       - `apps/dashboard/tests/e2e/today.spec.ts` (UI-01)
       - `apps/dashboard/tests/e2e/entity.spec.ts` (UI-02)
       - `apps/dashboard/tests/e2e/timeline.spec.ts` (ENT-08)
       - `apps/dashboard/tests/e2e/calendar.spec.ts` (UI-03)
       - `apps/dashboard/tests/integration/api-layer.test.ts` (SigV4 round-trip, test.todo-style)
       - `apps/dashboard/tests/integration/sse.test.ts` (NOTIFY→EventSource integration, test.todo)

    8. **Create 4 service-level test stubs** (Vitest, `it.todo(...)` style):
       - `services/dashboard-api/tests/merge-transactional.test.ts`
       - `services/dashboard-api/tests/merge-partial.test.ts`
       - `services/dashboard-api/tests/merge-resume.test.ts`
       - `services/dashboard-api/tests/timeline.test.ts`
       - `services/dashboard-listen-relay/tests/listen-reconnect.test.ts`
       - `services/dashboard-notify/tests/notify-payload.test.ts`

    9. **Create `.github/workflows/dashboard-ci.yml`**:
       ```yaml
       name: dashboard-ci
       on:
         pull_request:
           paths: ['apps/dashboard/**','services/dashboard-*/**','packages/contracts/**']
         push:
           branches: [master, main]
           paths: ['apps/dashboard/**','services/dashboard-*/**','packages/contracts/**']
       jobs:
         quick:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: pnpm/action-setup@v4
               with: { version: 9.12.0 }
             - uses: actions/setup-node@v4
               with: { node-version: 22.12.0, cache: 'pnpm' }
             - run: pnpm install --frozen-lockfile
             - run: pnpm --filter @kos/contracts typecheck
             - run: pnpm --filter @kos/dashboard typecheck
             - run: pnpm --filter @kos/dashboard test --run
             - run: pnpm --filter @kos/dashboard-api typecheck
             - run: pnpm --filter @kos/dashboard-api test --run
             - run: pnpm --filter @kos/dashboard-listen-relay typecheck
             - run: pnpm --filter @kos/dashboard-notify typecheck
         full:
           if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
           runs-on: ubuntu-latest
           needs: [quick]
           steps:
             - uses: actions/checkout@v4
             - uses: pnpm/action-setup@v4
               with: { version: 9.12.0 }
             - uses: actions/setup-node@v4
               with: { node-version: 22.12.0, cache: 'pnpm' }
             - run: pnpm install --frozen-lockfile
             - run: pnpm --filter @kos/dashboard exec playwright install --with-deps chromium
             - run: pnpm --filter @kos/dashboard build
             - run: pnpm --filter @kos/dashboard e2e
               env:
                 KOS_TEST_BEARER_TOKEN: ${{ secrets.KOS_TEST_BEARER_TOKEN }}
                 PLAYWRIGHT_BASE_URL: ${{ secrets.VERCEL_PREVIEW_URL }}
       ```

    10. **Populate 03-VALIDATION.md Per-Task Verification Map** — add rows for every task this plan introduces (T-03-W0-A, T-03-W0-B, T-03-W0-C) mapping Wave, Requirement, Threat Ref (from Plan 02 threat-model), Automated Command, File Exists. Set `nyquist_compliant: true` in frontmatter only after downstream plans all verify.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm --filter @kos/dashboard test --run && pnpm --filter @kos/dashboard-api test --run && pnpm --filter @kos/dashboard-listen-relay test --run && pnpm --filter @kos/dashboard-notify test --run && pnpm --filter @kos/dashboard exec playwright test --list</automated>
  </verify>
  <acceptance_criteria>
    - All 18 VALIDATION Wave-0 files exist (use this one-liner to count): `ls apps/dashboard/vitest.config.ts apps/dashboard/playwright.config.ts apps/dashboard/lighthouserc.json apps/dashboard/tests/unit/setup.ts apps/dashboard/tests/e2e/fixtures.ts apps/dashboard/tests/e2e/pwa-install.spec.ts apps/dashboard/tests/e2e/sse-reconnect.spec.ts apps/dashboard/tests/e2e/merge-audit.spec.ts apps/dashboard/tests/e2e/inbox-keyboard.spec.ts apps/dashboard/tests/e2e/auth-middleware.spec.ts apps/dashboard/tests/integration/api-layer.test.ts services/dashboard-api/tests/merge-transactional.test.ts services/dashboard-api/tests/merge-partial.test.ts services/dashboard-api/tests/merge-resume.test.ts services/dashboard-api/tests/timeline.test.ts services/dashboard-listen-relay/tests/listen-reconnect.test.ts services/dashboard-notify/tests/notify-payload.test.ts packages/contracts/src/dashboard.ts .github/workflows/dashboard-ci.yml | wc -l` equals 19 (18 files + contracts which was Task 2).
    - `grep -F "@kos/dashboard-api" services/dashboard-api/package.json` matches.
    - `grep -F "@kos/dashboard-listen-relay" services/dashboard-listen-relay/package.json` matches.
    - `grep -F "@kos/dashboard-notify" services/dashboard-notify/package.json` matches.
    - `grep -F "pg-listen" services/dashboard-listen-relay/package.json` matches (R-03 binding choice).
    - `pnpm --filter @kos/dashboard test --run` exits 0 (todo tests count as pass).
    - `pnpm --filter @kos/dashboard exec playwright test --list` prints 10+ specs without error.
    - `pnpm --filter @kos/dashboard-api test --run` exits 0.
    - `pnpm --filter @kos/dashboard-listen-relay test --run` exits 0.
    - `grep -F "runs-on: ubuntu-latest" .github/workflows/dashboard-ci.yml` matches.
    - 03-VALIDATION.md has non-empty rows in "Per-Task Verification Map" section (verify with `grep -c '^| T-03-' .planning/phases/03-dashboard-mvp/03-VALIDATION.md` ≥ 1).
  </acceptance_criteria>
  <done>
    All 18 Wave-0 gap files exist as stubs (todo/fixme); 3 new services scaffold and typecheck; CI workflow is valid YAML; VALIDATION.md map is seeded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev→monorepo | Developer local edits; no external data crosses here yet |
| CI→secrets | GitHub Actions reads `KOS_TEST_BEARER_TOKEN` + `VERCEL_PREVIEW_URL` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-00-01 | Information disclosure | `.github/workflows/dashboard-ci.yml` | mitigate | `KOS_TEST_BEARER_TOKEN` referenced via `${{ secrets.* }}`; never echoed; not `NEXT_PUBLIC_*` prefixed (RESEARCH §16). |
| T-3-00-02 | Tampering | `packages/contracts/src/dashboard.ts` | mitigate | Single source of truth; both Vercel + Lambda import same file → compile-time schema drift impossible. zod parse at every boundary (V5). |
| T-3-00-03 | Elevation | monorepo `@kos/dashboard` imports | accept | Linter rule added in Plan 01 prevents `apps/*` imports from `packages/*` inverse direction (RESEARCH §1 step 7). |
</threat_model>

<verification>
- `pnpm install` succeeds; lockfile updates cleanly.
- `pnpm -r typecheck` passes across the monorepo (no regression on Phase 1/2 packages).
- `pnpm --filter @kos/dashboard build` succeeds with 0 warnings on token file.
- Playwright spec list matches the 10 declared e2e specs.
</verification>

<success_criteria>
- 18 Wave-0 files from 03-VALIDATION.md exist.
- 3 service workspaces scaffold and typecheck.
- packages/contracts/src/dashboard.ts exports all schemas listed in must_haves.
- Dashboard CI workflow exists, parses as valid YAML, and references correct secrets.
- No Wave-1+ plan can execute until this plan's `<verify>` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-mvp/03-00-SUMMARY.md` with: files created, Wave-0 gaps closed (18/18), installed package versions (`next`, `react`, `tailwindcss`, `@playwright/test`, `vitest`, `@lhci/cli`, `pg-listen`, `aws4fetch` not yet), and a note that Plan 01 takes schema + shadcn init from here.
</output>
