---
phase: 06-granola-semantic-memory
plan: 09
type: execute
mode: gap_closure
wave: 2
depends_on: ["06-07", "06-08"]
files_modified:
  - .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md
autonomous: true
requirements:
  - AGT-04
gap_closure: true
gap_source: Re-verification after Plan 06-07 (AGT-04 wiring) and Plan 06-08 (INFO hardening)
gap_target: Confirm all closed; promote VERIFICATION.md status from gaps_found to verified
must_haves:
  truths:
    - "All Phase 6 unit tests still pass after Plans 06-07 and 06-08 changes"
    - "Gate verifier exits 0 in mock mode"
    - "E2E mock verifier exits 0"
    - "MEM-03 latency mock verifier exits 0"
    - "VERIFICATION.md gaps[].status for AGT-04 is 'closed' AND no other gaps remain in 'partial' or 'open'"
    - "VERIFICATION.md status frontmatter is 'verified' (5 human-required items remain marked HUMAN as expected — they are not gaps, they are post-deploy live-traffic measurements)"
  artifacts:
    - path: ".planning/phases/06-granola-semantic-memory/06-VERIFICATION.md"
      provides: "Re-verification report with AGT-04 closed; status promoted to verified"
      contains: "Plan 06-07"
  key_links:
    - from: "06-VERIFICATION.md"
      to: "Plan 06-07 + 06-08 closure evidence"
      via: "frontmatter gap status updates + body text closure markers"
      pattern: "closed_by:.*06-07-PLAN"
---

<objective>
Final re-verification after Plans 06-07 and 06-08 land. Run the full Phase 6 verification harness (gate verifier, e2e harness, MEM-03 latency, all relevant package tests). Update VERIFICATION.md to reflect that the AGT-04 PARTIAL gap is closed and the only remaining items are the 5 expected HUMAN verifications (AGT-06 quality, AGT-04 p95, MEM-04 p95, INF-10 cost, SC7 cache hit rate — all post-deploy).

Purpose: keep VERIFICATION.md as the single source of truth for Phase 6 completeness. Plan 06-07 already touches VERIFICATION.md inline (Task 3); this plan does the cross-plan reconciliation pass after 06-08 also lands and confirms no new regressions surfaced.

Output: VERIFICATION.md re-issued with status: verified, gaps[].status: closed for AGT-04, and a re-verification timestamp.

NOTE: This plan is OPTIONAL. The orchestrator (or developer) may choose to fold this into Plan 06-07 Task 3 if the wave dependency overhead is not worth a separate plan. If skipped, ensure Plan 06-07 Task 3 produces an equivalent VERIFICATION.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-granola-semantic-memory/06-VERIFICATION.md
@.planning/phases/06-granola-semantic-memory/06-07-PLAN.md
@.planning/phases/06-granola-semantic-memory/06-08-PLAN.md
</context>

<threat_model>
## Trust Boundaries

This plan only updates a documentation file (VERIFICATION.md). No new code paths, no new IAM, no new external calls. The only risk is misclassifying a still-open gap as closed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-VFY-01 | R (Repudiation / false closure) | VERIFICATION.md status promotion | mitigate | Acceptance criteria require automated re-runs of all verification scripts (gate, e2e, MEM-03 latency) PLUS package test suites BEFORE the status flip. If any exits non-zero, the status flip does NOT happen and Plan 06-09 reports remaining failures. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run all Phase 6 verification harnesses</name>
  <files>(no files modified — read-only verification phase)</files>
  <read_first>
    - .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md (current state — likely already partially updated by Plan 06-07 Task 3)
    - scripts/verify-phase-6-gate.mjs
    - scripts/verify-phase-6-e2e.mjs
    - scripts/verify-mem-03-latency.mjs
  </read_first>
  <action>
Run all verification harnesses in mock mode and capture exit codes.

Execute the following commands sequentially. If ANY exits non-zero, STOP. Do NOT proceed to Task 2. Surface the failures to the operator.

```bash
cd /home/ubuntu/projects/kevin-os
node scripts/verify-phase-6-gate.mjs --mock
node scripts/verify-phase-6-e2e.mjs --mock
node scripts/verify-mem-03-latency.mjs --mock
pnpm --filter @kos/azure-search test
pnpm --filter @kos/context-loader test
pnpm --filter @kos/service-dossier-loader test
pnpm --filter @kos/service-triage test
pnpm --filter @kos/service-voice-capture test
pnpm --filter @kos/service-entity-resolver test
pnpm --filter @kos/service-transcript-extractor test
pnpm --filter @kos/service-granola-poller test
pnpm --filter @kos/service-entity-timeline-refresher test
pnpm --filter @kos/cdk test
pnpm --filter @kos/db test
```

If ALL exit 0, proceed to Task 2.

If any non-zero exit, the operator must triage: typically a flaky test (re-run once), a Plan 06-08 oversight, or a real regression in 06-07. This plan blocks until the offending failure is resolved.
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/kevin-os && node scripts/verify-phase-6-gate.mjs --mock && node scripts/verify-phase-6-e2e.mjs --mock && node scripts/verify-mem-03-latency.mjs --mock && pnpm --filter @kos/azure-search test && pnpm --filter @kos/context-loader test && pnpm --filter @kos/service-dossier-loader test && pnpm --filter @kos/service-triage test && pnpm --filter @kos/service-voice-capture test && pnpm --filter @kos/service-entity-resolver test && pnpm --filter @kos/service-transcript-extractor test && pnpm --filter @kos/cdk test</automated>
  </verify>
  <acceptance_criteria>
    - Every command listed in the verify block exits 0
    - No flaky tests surface (re-run any single failure once; if it passes on retry, document as flake; if it fails twice, treat as regression and STOP)
  </acceptance_criteria>
  <done>All Phase 6 verification harnesses + package test suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Update VERIFICATION.md status frontmatter and add Re-verification section</name>
  <files>.planning/phases/06-granola-semantic-memory/06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md (full file, especially frontmatter)
  </read_first>
  <action>
Update VERIFICATION.md frontmatter and append a Re-verification section.

**Step 2.1 — Update YAML frontmatter:**

Edit `.planning/phases/06-granola-semantic-memory/06-VERIFICATION.md`:

1. Update `verified:` to current timestamp (use `date -u +"%Y-%m-%dT%H:%M:%SZ"` to capture)
2. Add a new field: `re_verified: <same-timestamp>`
3. Change `status:` from `gaps_found` to `verified`
4. Update `score:` to: `score: 7/7 roadmap success criteria auto-verified (5 require human + live-run evidence — all expected post-deploy items, not gaps)`
5. In `gaps:` array — confirm AGT-04 entry has `status: closed` and a `closed_by:` field referencing Plan 06-07. If Plan 06-07 Task 3 already wrote those fields, leave them. If not, add them now:
   ```yaml
       status: closed
       closed_by: ".planning/phases/06-granola-semantic-memory/06-07-PLAN.md (gap closure 2026-04-25)"
   ```

**Step 2.2 — Append Re-verification section:**

Append the following section after the existing "Gaps Summary" section, before any trailing italic verifier line:

```markdown
<!-- horizontal rule placeholder for Re-verification section -->

## Re-verification (Plan 06-09)

**Re-verified:** <timestamp from step 2.1>
**Trigger:** Plans 06-07 (AGT-04 azureSearch wiring) and 06-08 (REVIEW INFO hardening) shipped.

### What changed since initial verification

- **AGT-04 PARTIAL → VERIFIED.** Plan 06-07 wired hybridQuery from @kos/azure-search into all 4 consumer Lambdas (triage, voice-capture, entity-resolver, transcript-extractor) by passing azureSearch as a callable to loadContext(). CDK integrations-agents.ts injects AZURE_SEARCH_ADMIN_SECRET_ARN + AZURE_SEARCH_INDEX_NAME and grants bedrock:InvokeModel on Cohere v4 EU + secretsmanager:GetSecretValue on the Azure Search admin secret per consumer Lambda. 4 new wiring tests prevent regression.
- **REVIEW INFO findings closed.** Plan 06-08 closed IN-01 (UUID validation in hybridQuery), IN-02 (make_interval instead of `||' seconds'::interval`), IN-04 (local EBEvent interface in dossier-loader handler), IN-05 (Crockford ULID alphabet in trigger-full-dossier.mjs), IN-06 (verified GEMINI_FULL_DOSSIER_TTL_SECONDS constant — already in place per 06-REVIEW-FIX.md WR-04). IN-03 (RDS rejectUnauthorized) explicitly skipped per REVIEW.md "No action required for Phase 6".

### Re-verification harnesses re-run

| Harness | Command | Result |
|---------|---------|--------|
| Gate verifier | `node scripts/verify-phase-6-gate.mjs --mock` | exit 0 |
| E2E quality multiplier | `node scripts/verify-phase-6-e2e.mjs --mock` | exit 0 |
| MEM-03 latency | `node scripts/verify-mem-03-latency.mjs --mock` | exit 0 |
| @kos/azure-search tests | `pnpm --filter @kos/azure-search test` | exit 0 |
| @kos/context-loader tests | `pnpm --filter @kos/context-loader test` | exit 0 |
| @kos/service-dossier-loader tests | `pnpm --filter @kos/service-dossier-loader test` | exit 0 |
| @kos/service-triage tests | `pnpm --filter @kos/service-triage test` | exit 0 |
| @kos/service-voice-capture tests | `pnpm --filter @kos/service-voice-capture test` | exit 0 |
| @kos/service-entity-resolver tests | `pnpm --filter @kos/service-entity-resolver test` | exit 0 |
| @kos/service-transcript-extractor tests | `pnpm --filter @kos/service-transcript-extractor test` | exit 0 |
| @kos/cdk tests | `pnpm --filter @kos/cdk test` | exit 0 |

### Remaining items

The 5 HUMAN-flagged items (AGT-06 action item quality, AGT-04 p95 latency, MEM-04 p95 at 100k rows, INF-10 cost per call, SC7 dossier cache hit rate) are all post-deploy live-traffic measurements. They are documented in 06-06-GATE-evidence-template.md for Kevin to fill in after deploy + 1 day of production traffic. They are NOT gaps — they are expected human acceptance steps.

**Phase 6 status: VERIFIED.** Ready for production deploy + post-deploy human acceptance.

<!-- horizontal rule placeholder for footer -->

_Re-verified: 2026-04-25_
_Re-verifier: Claude (Plan 06-09)_
```

**Step 2.3 — Final sanity grep:**

After saving, confirm:
- `grep -c "status: verified" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns 1
- `grep -c "Plan 06-07" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns >= 3
- `grep -c "Plan 06-08" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns >= 1
- `grep -c "## Re-verification" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns 1
  </action>
  <verify>
    <automated>cd /home/ubuntu/projects/kevin-os && grep -q "^status: verified" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md && grep -c "Plan 06-07" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md && grep -c "## Re-verification" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: verified` (not `gaps_found`)
    - Frontmatter `gaps[].status` for AGT-04 is `closed`
    - Body has a `## Re-verification (Plan 06-09)` section
    - `grep -c "Plan 06-07" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns >= 3
    - `grep -c "Plan 06-08" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns >= 1
    - Original PARTIAL prose preserved (chronological record intact — the Re-verification section adds context, doesn't delete history)
  </acceptance_criteria>
  <done>VERIFICATION.md status promoted to verified; Re-verification section appended; original history preserved.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -E "^status: verified" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns 1 line
2. `grep -c "## Re-verification (Plan 06-09)" .planning/phases/06-granola-semantic-memory/06-VERIFICATION.md` returns 1
3. `node scripts/verify-phase-6-gate.mjs --mock` still exits 0
4. Original "PARTIAL" prose for AGT-04 still present in body (chronological history preserved); only the frontmatter status flipped + closure markers added.
</verification>

<success_criteria>
- All verification harnesses re-run green
- VERIFICATION.md frontmatter `status: verified`
- AGT-04 gap entry has `status: closed` and `closed_by:` referencing Plan 06-07
- Re-verification section appended with traceable reference to Plans 06-07 and 06-08
- 5 expected HUMAN items remain documented (post-deploy acceptance — not gaps)
- Phase 6 declared VERIFIED end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/06-granola-semantic-memory/06-09-SUMMARY.md` with:
- Plan ID, re-verification timestamp
- All harness exit codes recorded
- VERIFICATION.md status transition (gaps_found → verified)
- Final note: "Phase 6 ready for production deploy. Post-deploy: Kevin fills 06-06-GATE-evidence-template.md with 5 HUMAN measurements."
</output>
