---
phase: 10-migration-decommission
plan: 01
type: execute
wave: 1
depends_on: [10-00]
files_modified:
  - services/vps-classify-migration/src/handler.ts
  - services/vps-classify-migration/src/hmac.ts
  - services/vps-classify-migration/src/emit.ts
  - services/vps-classify-migration/test/handler.test.ts
  - services/vps-classify-migration/test/hmac.test.ts
  - scripts/verify-classify-substance.mjs
  - scripts/.fixtures/verify-classify-substance-prompt.txt
  - .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "classify-adapter Lambda accepts old VPS payload shape + HMAC-validates + emits capture.received to kos.capture bus"
    - "Lambda Function URL is the new endpoint replacing VPS classify webhook URL"
    - "scripts/verify-classify-substance.mjs samples 10 Legacy Inbox rows per script over last 7 days + generates Gemini 2.5 Pro equivalence report + operator hand-review checklist"
    - "10-01-CUTOVER-RUNBOOK.md documents the T-0 DNS/webhook flip from VPS URL → Lambda Function URL with rollback at T+30min"
  artifacts:
    - path: services/vps-classify-migration/src/handler.ts
      provides: Full HMAC + payload translation + capture.received emit
      min_lines: 80
    - path: services/vps-classify-migration/src/hmac.ts
      provides: HMAC-SHA256 validation with timing-safe compare
    - path: services/vps-classify-migration/src/emit.ts
      provides: EventBridge PutEvents for capture.received
    - path: scripts/verify-classify-substance.mjs
      provides: 7-day parity checker via Gemini 2.5 Pro + operator checklist
      min_lines: 200
    - path: .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md
      provides: T-0 cutover procedure + T+30min rollback
  key_links:
    - from: services/vps-classify-migration/src/handler.ts
      to: kos.capture EventBridge bus
      via: PutEventsCommand with detail-type capture.received
    - from: services/vps-classify-migration/src/hmac.ts
      to: AWS Secrets Manager
      via: GetSecretValue for HMAC_SECRET_ARN
    - from: scripts/verify-classify-substance.mjs
      to: Vertex AI Gemini 2.5 Pro (per Locked #3 revision + INF-10)
      via: "@google-cloud/vertexai generateContent call"
    - from: scripts/verify-classify-substance.mjs
      to: Notion Legacy Inbox DB
      via: notion.databases.query with [MIGRERAD] filter + last 7 days
---

<objective>
Implement MIG-01 Part 1 — the classify_and_save thin adapter Lambda that keeps the VPS webhook URL contract during the 14-day decom overlap. The adapter translates the old payload shape into a KOS-native `capture.received` event on `kos.capture` EventBridge bus.

Also deliver the 7-day same-substance verifier that compares 10 Legacy Inbox rows (VPS output preserved by Phase 1 freeze) against the new Lambda output using Gemini 2.5 Pro as a judge + operator hand-review gate.

Purpose: preserve classify functionality during migration + produce the Gate 10 SC 1 evidence artifact.

Output: working Lambda + verifier script + cutover runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-migration-decommission/10-CONTEXT.md
@.planning/phases/10-migration-decommission/10-RESEARCH.md
@.planning/phases/10-migration-decommission/10-VALIDATION.md
@.planning/phases/10-migration-decommission/10-00-PLAN.md
@services/vps-freeze-patched/classify_and_save.py
@services/triage/src/handler.ts
@services/voice-capture/src/handler.ts

<interfaces>
From @kos/contracts (added in 10-00):
```typescript
export const ClassifyPayloadSchema = z.object({
  title: z.string().optional(),
  subject: z.string().optional(),
  is_duplicate: z.boolean().optional(),
  already_processed: z.boolean().optional(),
}).passthrough();
export type ClassifyPayload = z.infer<typeof ClassifyPayloadSchema>;
```

From existing services/triage and Phase 2 contract (capture.received event shape):
```typescript
// EventBridge detail for capture.received:
// {
//   capture_id: string (ULID),
//   kind: 'text' | 'voice' | 'email' | 'highlight',
//   channel: 'telegram' | 'ios' | 'email' | 'discord' | 'whatsapp' | 'linkedin' | 'vps-classify-migration',
//   raw: { ... original payload ... },
//   received_at: string (ISO),
// }
```

From Phase 4 HMAC pattern (services/ios-webhook):
```typescript
// HMAC-SHA256(secret, body + timestamp) header pattern
// Reject if timestamp > 300s skew
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify adapter handler + HMAC + emit</name>
  <files>
    services/vps-classify-migration/src/handler.ts,
    services/vps-classify-migration/src/hmac.ts,
    services/vps-classify-migration/src/emit.ts,
    services/vps-classify-migration/test/handler.test.ts,
    services/vps-classify-migration/test/hmac.test.ts
  </files>
  <behavior>
    - Test 1 (hmac): valid signature passes
    - Test 2 (hmac): invalid signature returns 401
    - Test 3 (hmac): timestamp skew > 300s returns 401
    - Test 4 (hmac): replay within 300s passes (dedup happens upstream via capture_id ULID)
    - Test 5 (handler): old-shape payload { title, is_duplicate: true } maps to capture.received with channel=vps-classify-migration
    - Test 6 (handler): emits to kos.capture bus with PutEvents
    - Test 7 (handler): Zod validation failure returns 400
    - Test 8 (handler): Sentry capture on emit failure
  </behavior>
  <action>
    **`src/hmac.ts`**: Export `validateHmac(body: string, timestamp: string, signature: string, secret: string): boolean`. Use `crypto.timingSafeEqual`. Reject if abs(now - timestamp) > 300_000 ms. Reuse pattern from `services/ios-webhook` (Phase 4) — if that file hasn't landed yet because Phase 4 isn't executed, inline the HMAC pattern from Phase 4 RESEARCH.md.

    **`src/emit.ts`**: Export `emitCaptureReceived(deps: { eb: EventBridgeClient, busName: string }, event: { capture_id: string, raw: unknown }): Promise<void>`. Uses `PutEventsCommand` with `Source: 'kos.capture-migration-adapter'`, `DetailType: 'capture.received'`, full event detail per the capture.received contract.

    **`src/handler.ts`**:
    1. Parse Lambda Function URL event.
    2. Read HMAC_SECRET from cached Secrets Manager value.
    3. `validateHmac(event.body, event.headers['x-timestamp'], event.headers['x-signature'], secret)` → 401 on fail.
    4. Parse body through `ClassifyPayloadSchema` → 400 on fail.
    5. Generate capture_id (ULID).
    6. Call `emitCaptureReceived({eb, busName: process.env.KOS_CAPTURE_BUS_NAME}, {capture_id, raw: parsedPayload})`.
    7. Return 202 { capture_id, adapter_version: '10-01-v1' }.
    8. Wrap in `withSentry` handler wrapper.

    **Test file**: 8 vitest cases covering the 8 behaviors above using `aws-sdk-client-mock` for EventBridgeClient + SecretsManagerClient.

    **Pay attention to:** The VPS classify_and_save.py signed the old webhook request with a shared secret — Wave 1 must use the **same** secret as the old VPS webhook (stored in AWS Secrets Manager as `kos/vps-classify-webhook-hmac`). If no such secret exists yet (Phase 1 may not have seeded it), generate a fresh one and document in cutover runbook that Kevin must paste it into the VPS-side caller before the cutover minute.
  </action>
  <verify>
    <automated>pnpm --filter @kos/service-vps-classify-migration test -- --run && pnpm --filter @kos/service-vps-classify-migration typecheck</automated>
  </verify>
  <done>
    8/8 vitest cases pass; typecheck clean; handler.ts is 80+ lines with full HMAC + Zod + emit + Sentry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Same-substance verifier (Gemini 2.5 Pro judge + operator hand-review)</name>
  <files>
    scripts/verify-classify-substance.mjs,
    scripts/.fixtures/verify-classify-substance-prompt.txt
  </files>
  <action>
    **`scripts/verify-classify-substance.mjs`** (ESM Node script, ~250 lines):

    Flow:
    1. **Input args:** `--since YYYY-MM-DD` (default: 7 days ago), `--sample-size N` (default: 10), `--script classify|morning|evening` (required).
    2. **Sample selection:** Query Notion Legacy Inbox DB filtered by `Source` select = argument script + `CreatedAt` within window. Take last N rows.
    3. **Re-run through new adapter:** For each Legacy Inbox row, extract `OriginalPayload` JSON field, POST it to the new Lambda Function URL (for classify) OR to the Phase 7 morning-brief/day-close Lambda triggers (for morning/evening — call `lambda:Invoke` directly on those functions). Capture the result.
       - For classify: result = the new capture.received detail + any downstream row written by triage/voice-capture.
       - For morning/evening: result = the new Notion daily-brief-log row written by AUTO-01/AUTO-03.
    4. **Generate comparison pairs:** For each (legacy_row, new_output), build a markdown diff section.
    5. **Gemini 2.5 Pro judge:** POST each pair to Vertex AI `gemini-2.5-pro` endpoint with the prompt from `scripts/.fixtures/verify-classify-substance-prompt.txt`:
       ```
       You are judging migration parity between a legacy Python script's output
       and a new AWS Lambda's output for the same input. They should produce
       "equivalent in substance" outputs.

       Rate equivalence on 0..1:
       - 1.0 = fully equivalent (same intent, same fields, minor formatting diff ok)
       - 0.8-0.99 = equivalent with documented drift
       - 0.5-0.79 = partial drift, operator MUST review
       - < 0.5 = substantial drift, likely migration defect

       Output JSON: { score: number, drift_summary: string, verdict: "PASS" | "OPERATOR_REVIEW" | "FAIL" }

       Input pair:
       --- LEGACY ---
       {legacy_content}
       --- NEW ---
       {new_content}
       ```
    6. **Output report:** `.planning/phases/10-migration-decommission/10-01-substance-report-{script}-{date}.md` with:
       - Summary table: pair_number | score | verdict
       - Full per-pair sections with legacy + new + Gemini verdict + Gemini drift_summary
       - Operator hand-review checklist: 10 checkboxes per script (pair 1..10) with space for Kevin to write PASS/FAIL + note
    7. **Exit codes:**
       - 0: all 10 Gemini scores >= 0.8 AND operator_review_checklist is present but unfilled
       - 1: Gemini flagged ANY pair < 0.5 OR sample size < 10
       - 2: Notion query returned < 10 rows (cannot sample)
    8. **Dry-run mode** (`--dry-run`): skip Gemini call; output pairs only.

    **Dependencies:** `@notionhq/client`, `@google-cloud/vertexai`, `@aws-sdk/client-lambda`, `@aws-sdk/client-eventbridge`, dotenv. Read Vertex SA creds from local `.gcp-sa-key.json` (documented in deferred-items.md from Phase 6).

    **`scripts/.fixtures/verify-classify-substance-prompt.txt`**: The prompt text as above, verbatim.

    **Sample corpus prereq (P-08 from RESEARCH):** If Legacy Inbox has < 10 rows for the target script in the last 7 days, exit 2 with a human-readable warning + suggestion to run VPS scripts manually via `ssh kevin@98.91.6.66 'sudo systemctl restart <unit>'` to generate sample inputs.

    **CRITICAL:** This script never writes to production Notion / RDS — read-only and Lambda:Invoke only.
  </action>
  <verify>
    <automated>node --check scripts/verify-classify-substance.mjs && grep -q '@google-cloud/vertexai' scripts/verify-classify-substance.mjs && grep -q 'operator_review_checklist' scripts/verify-classify-substance.mjs && grep -q 'gemini-2.5-pro' scripts/verify-classify-substance.mjs && grep -q '--dry-run' scripts/verify-classify-substance.mjs && test -f scripts/.fixtures/verify-classify-substance-prompt.txt</automated>
  </verify>
  <done>
    Script lints clean via `node --check`; has Vertex AI integration, operator checklist generation, and dry-run mode. Script is NOT executed during Phase 10 planning — operator runs it during 7-day window.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cutover runbook (T-0 DNS/webhook flip)</name>
  <files>.planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md</files>
  <action>
    Document the atomic cutover procedure:

    **Pre-cutover (T-24h to T-0):**
    1. CDK deploy `KosMigration` → capture Lambda Function URL.
    2. Seed `kos/vps-classify-webhook-hmac` secret in Secrets Manager (generate new if missing).
    3. Paste HMAC secret into VPS classify caller (SSH: edit config, `systemctl restart`).
    4. Verify Lambda health: `curl -X POST <fn-url> -H 'x-timestamp: <now>' -H 'x-signature: <hmac>' -d '{"title":"healthcheck"}'` returns 202.

    **T-0 cutover (< 30 seconds):**
    1. SSH `kevin@98.91.6.66`; `sudo systemctl stop <classify-unit>`.
    2. SSH: flip the VPS cron / n8n webhook URL env var to the Lambda Function URL (where upstream callers reach the old URL).
    3. Local: `node scripts/verify-classify-lambda-health.mjs` (check 5 test POSTs over 30s).

    **T+5min verification:**
    1. `node scripts/verify-classify-substance.mjs --script classify --since T` (picks up last 5 min of real traffic).
    2. Spot-check Kevin's Notion Command Center for new rows from adapter.

    **Rollback at T+30min if SC 1 regression:**
    1. SSH: flip VPS URL env back to old (pre-cutover) value.
    2. SSH: `sudo systemctl start <classify-unit>` (Phase 1 freeze redirects to Legacy Inbox — safe).
    3. CDK: leave Lambda running (idle); don't delete (reversibility floor).
    4. Open incident ticket with cutover transcript.

    **Day 7 gate:** `scripts/verify-classify-substance.mjs --script classify` produces report; Kevin reviews; 10/10 PASS = SC 1 closed for classify.

    Include placeholders: `# DRY_RUN_EVIDENCE:` — operator pastes transcript here after dry-run (for gate audit).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md && grep -q 'T-0' .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md && grep -q 'rollback' .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md && grep -q 'DRY_RUN_EVIDENCE' .planning/phases/10-migration-decommission/10-01-CUTOVER-RUNBOOK.md</automated>
  </verify>
  <done>
    Runbook exists, documents T-0 + T+30min rollback + evidence placeholder. No automation attempted — pure operator reference.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:
- vps-classify-migration Lambda has full handler (HMAC + Zod + emit + Sentry) with 8 passing tests.
- CDK `KosMigration` synths with the Lambda wired to kos.capture bus.
- `scripts/verify-classify-substance.mjs` lints clean + has Vertex AI integration.
- Cutover runbook exists.
- **Not executed here:** the actual cutover (operator runs it), the 7-day verification (operator runs it).
</verification>

<success_criteria>
- MIG-01 Part 1 (classify_and_save adapter) code-complete
- Gemini 2.5 Pro same-substance verifier ready for operator use
- Cutover runbook documents T-0 + rollback
- SC 1 evidence pipeline established (will fill in during 7-day window)
</success_criteria>

<output>
After completion, create `.planning/phases/10-migration-decommission/10-01-SUMMARY.md` documenting: adapter handler size, test counts, verifier integration points, cutover runbook T-0 steps, operator-deferred items (HMAC secret seeding, VPS URL flip, Gemini SA key availability, 10 cutover dry-run transcripts).
</output>
