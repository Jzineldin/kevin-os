---
phase: 10-migration-decommission
plan: 04
type: execute
wave: 2
depends_on: [10-00]
files_modified:
  - services/discord-brain-dump/src/handler.ts
  - services/discord-brain-dump/src/poller.ts
  - services/discord-brain-dump/src/emit.ts
  - services/discord-brain-dump/test/handler.test.ts
  - services/discord-brain-dump/test/poller.test.ts
  - scripts/verify-discord-brain-dump-substance.mjs
  - .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md
autonomous: true
requirements: [CAP-10]
must_haves:
  truths:
    - "services/discord-brain-dump/handler polls Discord #brain-dump channel every 5 min (EventBridge Scheduler)"
    - "cursor-based idempotency: only NEW messages since last run are forwarded"
    - "each new message published as capture.received to kos.capture with channel=discord, kind=text"
    - "7-day same-substance verifier compares old VPS brain-dump-listener output vs new Lambda output"
  artifacts:
    - path: services/discord-brain-dump/src/handler.ts
      provides: EventBridge Scheduler-triggered Lambda handler
      min_lines: 60
    - path: services/discord-brain-dump/src/poller.ts
      provides: Discord REST client for /channels/{id}/messages with pagination
      min_lines: 100
    - path: services/discord-brain-dump/src/emit.ts
      provides: capture.received emit (reusable shape like Plan 10-01)
    - path: scripts/verify-discord-brain-dump-substance.mjs
      provides: 7-day parity checker
    - path: .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md
      provides: operator cutover steps
  key_links:
    - from: services/discord-brain-dump/src/poller.ts
      to: Discord REST API (discord.com)
      via: GET /api/v10/channels/{channel_id}/messages?after={cursor}&limit=100
    - from: services/discord-brain-dump/src/handler.ts
      to: kos.capture EventBridge bus
      via: PutEventsCommand detail-type=capture.received channel=discord kind=text
    - from: services/discord-brain-dump/src/handler.ts
      to: DynamoDB DiscordBrainDumpCursor
      via: getCursor/setCursor (from 10-00 scaffold)
---

<objective>
Implement CAP-10 — migrate `brain-dump-listener` from VPS Python script to AWS Lambda. Per RESEARCH §3 + D-21, use Lambda polling (not webhook inbound / not bot WebSocket) matching Phase 5 SC 4 shape.

Purpose: retire the VPS brain-dump-listener process (one of 4 unfrozen scripts) + keep Discord #brain-dump as fallback capture per CAP-10.

Output: working Lambda + 7-day same-substance verifier + cutover runbook. Runs in Wave 2 parallel with Plan 10-03 (Brain DB archive) — no file conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-migration-decommission/10-CONTEXT.md
@.planning/phases/10-migration-decommission/10-RESEARCH.md
@.planning/phases/10-migration-decommission/10-VALIDATION.md
@.planning/phases/10-migration-decommission/10-00-PLAN.md

<interfaces>
From @kos/contracts (10-00):
```typescript
DiscordChannelMessageSchema = z.object({
  id: z.string(),
  channel_id: z.string(),
  author: z.object({ id: z.string(), username: z.string(), bot: z.boolean().optional() }),
  content: z.string(),
  timestamp: z.string().datetime(),
});
```

From services/discord-brain-dump/src/cursor.ts (10-00):
```typescript
export async function getCursor(channelId: string): Promise<string | null>;
export async function setCursor(channelId: string, messageId: string): Promise<void>;
```

Discord REST API:
```
GET https://discord.com/api/v10/channels/{channel_id}/messages?after={message_id}&limit=100
Headers: Authorization: Bot <DISCORD_BOT_TOKEN>
Returns: DiscordChannelMessage[]
```

capture.received detail shape (Phase 2 contract):
```typescript
{
  capture_id: string (ULID),
  kind: 'text',
  channel: 'discord',
  raw: { discord_message_id, channel_id, author, content, timestamp },
  received_at: string (ISO),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discord poller implementation (REST client + cursor + emit)</name>
  <files>
    services/discord-brain-dump/src/poller.ts,
    services/discord-brain-dump/src/emit.ts,
    services/discord-brain-dump/test/poller.test.ts
  </files>
  <behavior>
    - Test 1: fresh cursor (null) → fetch /messages?limit=100 (no `after`), emit ALL up to limit
    - Test 2: existing cursor → fetch /messages?after={cursor}, emit only new ones
    - Test 3: bot-authored messages FILTERED OUT (don't forward our own bot's messages if KOS ever posts)
    - Test 4: 401/403 response → throw explicit auth-gate error
    - Test 5: 429 rate-limited → respect `Retry-After` header, retry once, fail gracefully if still rate-limited
    - Test 6: pagination — if 100 messages returned, next call uses oldest's ID as `after` (Discord returns newest-first by default, use `before` for forward-walking)
    - Test 7: emit shape matches capture.received contract
  </behavior>
  <action>
    **`src/poller.ts`** (~120 lines):
    - Export `fetchNewMessages(channelId: string, cursor: string | null, botToken: string): Promise<DiscordChannelMessage[]>`.
    - Use `fetch()` to Discord REST v10 endpoint.
    - **Cursor semantics:** Discord's `?after=<id>` returns messages AFTER the ID, **oldest-first**, up to `limit=100`. So each poll cycle iterates until page returns < 100 results OR safety cap of 500 messages/run (avoid infinite loop).
    - Filter out `author.bot === true` unless `INCLUDE_BOTS` env set.
    - Throw `DiscordAuthError`, `DiscordRateLimitError`, `DiscordTransientError` per status code.
    - Handle `Retry-After` on 429: sleep, retry once.

    **`src/emit.ts`** (~40 lines):
    - Export `emitDiscordCapture(deps: { eb, busName }, msg: DiscordChannelMessage): Promise<string>` (returns capture_id).
    - Generate ULID; build capture.received detail; PutEvents.
    - Source: 'kos.capture-discord-brain-dump'.

    **`test/poller.test.ts`** (~150 lines):
    - 7 vitest cases using `msw` (mock service worker) or `undici.MockAgent` to simulate Discord HTTPS responses.
    - Include a fixture from `packages/test-fixtures/phase-10/discord-message.json`.
    - Test 6 specifically: create a mock that returns 100 msgs on first call + 50 on second — asserts next-cursor is oldest of second response.
  </action>
  <verify>
    <automated>pnpm --filter @kos/service-discord-brain-dump test -- --run poller && pnpm --filter @kos/service-discord-brain-dump typecheck</automated>
  </verify>
  <done>
    poller.test.ts passes 7/7; typecheck clean; `fetchNewMessages` handles auth/rate-limit/pagination; emit publishes to kos.capture.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler orchestration + DLQ + deployment wiring</name>
  <files>
    services/discord-brain-dump/src/handler.ts,
    services/discord-brain-dump/test/handler.test.ts
  </files>
  <behavior>
    - Test 1: EventBridge Scheduler event → read cursor → call poller → emit each msg → advance cursor
    - Test 2: 0 new messages → cursor unchanged; no PutEvents called
    - Test 3: partial failure — emit for msg 1 succeeds, msg 2 throws — cursor advances to msg 1's ID; msg 2 and beyond retried next cycle
    - Test 4: DiscordAuthError → Sentry.captureException + don't advance cursor + rethrow (Lambda fails, surfaces in CloudWatch)
    - Test 5: Sentry context captured with channel_id + cursor_before
  </behavior>
  <action>
    **`src/handler.ts`** (~80 lines):
    1. Read `DISCORD_BRAIN_DUMP_CHANNEL_ID` env var.
    2. Read `DISCORD_BOT_TOKEN` from Secrets Manager (cached across invocations).
    3. `cursor = await getCursor(channelId)`.
    4. `messages = await fetchNewMessages(channelId, cursor, botToken)`.
    5. For each msg in messages (oldest-first):
       - Try `emitDiscordCapture({eb, busName}, msg)`.
       - On success: `await setCursor(channelId, msg.id)`.
       - On failure: break loop, rethrow (cursor stays at last successfully emitted ID).
    6. Return { processed: N, cursor_after: latestSuccessfulId }.
    7. Wrap in `withSentry` handler.
    8. DLQ: EventBridge Scheduler rule has `dead_letter_config` pointing at a SQS DLQ (CDK additive in this plan, update `packages/cdk/lib/stacks/integrations-migration.ts`).

    **`test/handler.test.ts`** (~150 lines):
    - 5 vitest cases using aws-sdk-client-mock for EventBridge + DynamoDB.
    - Mock poller via ESM module mock.

    **CDK update:** Add SQS DLQ to `discord-brain-dump-poll` scheduler rule in `integrations-migration.ts`. Test: `pnpm --filter @kos/cdk test -- --run integrations-migration.discord-dlq` expects the DLQ + 3 retry policy.
  </action>
  <verify>
    <automated>pnpm --filter @kos/service-discord-brain-dump test -- --run handler && pnpm --filter @kos/cdk test -- --run integrations-migration && pnpm --filter @kos/service-discord-brain-dump typecheck</automated>
  </verify>
  <done>
    handler test passes 5/5; CDK DLQ test passes; handler advances cursor only on successful emit.
  </done>
</task>

<task type="auto">
  <name>Task 3: 7-day same-substance verifier + cutover runbook</name>
  <files>
    scripts/verify-discord-brain-dump-substance.mjs,
    .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md
  </files>
  <action>
    **`scripts/verify-discord-brain-dump-substance.mjs`** (~200 lines):

    Parity window: 48-168 hours.

    1. **Source A (legacy):** SSH to VPS, `grep` logs of `brain-dump-listener` for last 7 days — extract forwarded message IDs + forwarded content. If logs are lost, fall back to querying whatever KOS webhook destination the VPS script was hitting (likely a Notion DB or S3 key).
    2. **Source B (new Lambda):** Query RDS `mention_events` or the Phase 2 `capture_log` (if exists) filtered by `channel='discord'` over the same window.
    3. **Diff:** Left-join on Discord message ID. Report:
       - messages in A but not B: potentially LOST by new Lambda (investigate)
       - messages in B but not A: potentially DOUBLE-FORWARDED (investigate)
       - messages in both: verify content equality (str-eq)
    4. **Gemini 2.5 Pro judge:** For each content mismatch, send (old, new) pair to Vertex AI (reuse prompt from 10-01).
    5. **Operator hand-review checklist:** 10 sample messages — Kevin confirms content/routing parity.
    6. **Output report:** `.planning/phases/10-migration-decommission/10-04-discord-substance-report-{date}.md`.
    7. **Exit codes:** 0 = all reconciled + Gemini scores ≥ 0.8; 1 = any loss/doubling; 2 = insufficient data.

    Note: if the old brain-dump-listener.py has no structured logs, this verifier can't run retroactively. **Pre-cutover required action:** Run `ssh kevin@98.91.6.66 'cat /opt/kos-vps/brain-dump-listener.py'` FIRST to see what it logs; may need to patch it to JSON-log messages for 7 days BEFORE cutover. Document this in the runbook.

    **`10-04-DISCORD-CUTOVER-RUNBOOK.md`** (operator reference):

    **Pre-cutover (T-7d):**
    1. Discover brain-dump-listener via `bash scripts/discover-vps-scripts.sh` (Plan 10-06 prereq) — get unit name + Discord channel_id + bot token location.
    2. If brain-dump-listener.py doesn't JSON-log messages, patch it to do so for the 7-day sample window. Commit the patched version; push back to VPS.
    3. Seed `kos/discord-bot-token` secret with the same bot token the VPS uses (or a fresh one — documented in runbook).
    4. Deploy `KosMigration` stack if not already deployed (CDK).

    **T-0 cutover (~2 min):**
    1. Retire VPS brain-dump-listener: `bash scripts/retire-vps-script.sh --unit brain-dump-listener --replaced-by discord-brain-dump-lambda`.
    2. Wait 5 min; check Lambda CloudWatch logs for first poll success.
    3. Send test message in Discord #brain-dump.
    4. Within 5 min: verify `capture.received` event detail appears in kos.capture bus (query via `aws logs filter-log-events` on the triage Lambda log group).

    **T+7d gate:**
    1. `node scripts/verify-discord-brain-dump-substance.mjs` → expect PASS.
    2. Kevin reviews 10-sample checklist.

    **Rollback (within 24h of cutover):**
    1. `bash scripts/retire-vps-script.sh --undo --unit brain-dump-listener`.
    2. Disable Discord Lambda via CDK context flag OR EventBridge `disable-schedule`.

    Include `# DRY_RUN_EVIDENCE:` placeholder.
  </action>
  <verify>
    <automated>node --check scripts/verify-discord-brain-dump-substance.mjs && grep -q 'discord' scripts/verify-discord-brain-dump-substance.mjs && grep -q 'VPS.*brain-dump-listener\|ssh' scripts/verify-discord-brain-dump-substance.mjs && test -f .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md && grep -q 'brain-dump-listener' .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md && grep -q 'DRY_RUN_EVIDENCE' .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md && grep -q 'retire-vps-script.sh' .planning/phases/10-migration-decommission/10-04-DISCORD-CUTOVER-RUNBOOK.md</automated>
  </verify>
  <done>
    Verifier + runbook present; verifier covers lost/doubled/mismatched messages; runbook has pre-cutover patching note + T-0 + rollback.
  </done>
</task>

</tasks>

<verification>
Post-plan:
- Discord Lambda has full polling + cursor + emit + DLQ.
- 7 poller tests + 5 handler tests + existing 3 cursor tests = 15 passing.
- Verifier + runbook present.
</verification>

<success_criteria>
- CAP-10 Discord brain-dump-listener migration code-complete
- Lambda polls every 5 min per Phase 5 SC 4
- Cursor-based idempotency prevents dupes
- 7-day same-substance verifier ready for operator
</success_criteria>

<output>
After completion, create `.planning/phases/10-migration-decommission/10-04-SUMMARY.md` documenting: poller auth/rate-limit/pagination strategy, cursor idempotency semantics, DLQ on failures, substance verifier approach, operator-deferred items (Discord bot token seeding, brain-dump-listener.py pre-cutover log patching, channel ID + bot token provisioning).
</output>
