---
phase: 11
plan: 7
type: execute
wave: 3
depends_on: [2, 3, 4, 5, 6]
files_modified:
  - apps/dashboard/src/lib/button-registry.ts
  - apps/dashboard/src/app/(app)/layout.tsx
  - apps/dashboard/src/components/app-shell/Sidebar.tsx
  - apps/dashboard/src/app/(app)/chat/page.tsx
  - apps/dashboard/src/app/(app)/settings/page.tsx
  - apps/dashboard/src/app/(app)/today/Composer.tsx
  - apps/dashboard/src/app/(app)/today/PriorityList.tsx
  - apps/dashboard/src/app/(app)/today/DraftsCard.tsx
  - apps/dashboard/src/app/(app)/today/DroppedThreads.tsx
  - apps/dashboard/src/app/(app)/inbox/InboxClient.tsx
  - apps/dashboard/src/app/(app)/inbox/ItemDetail.tsx
  - apps/dashboard/src/app/(app)/entities/page.tsx
  - apps/dashboard/src/app/(app)/calendar/CalendarWeekView.tsx
  - .planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-BUTTON-AUDIT.md
autonomous: false
requirements: [REQ-3, REQ-12]

must_haves:
  truths:
    - "Every interactive surface (button, link, kbd, tab, filter, search) in the (app) route group is audited"
    - "Each surface either has a working handler OR has been removed (D-06)"
    - "Audit catalogued in .planning/phases/11-.../11-BUTTON-AUDIT.md (one row per surface, KEEP/WIRE/REMOVE)"
    - "ChatBubble mounted globally in (app)/layout.tsx — visible on every page"
    - "Sidebar Chat link enabled (no longer disabled with 'Ships with Phase 4' tooltip)"
    - "Sidebar Settings link decided: wired to a useful surface OR removed"
    - "/chat page shell exists with placeholder for Phase 11-ter"
    - "BUTTON_REGISTRY populated with every audited data-testid for the e2e parametric test"
  artifacts:
    - path: ".planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-BUTTON-AUDIT.md"
      provides: "Per-surface audit table with verdict + handler + data-testid"
      contains: "KEEP"
    - path: "apps/dashboard/src/lib/button-registry.ts"
      provides: "Populated BUTTON_REGISTRY with every audited surface"
      min_lines: 30
    - path: "apps/dashboard/src/app/(app)/layout.tsx"
      provides: "ChatBubble mounted globally below Toaster"
      contains: "ChatBubble"
    - path: "apps/dashboard/src/app/(app)/chat/page.tsx"
      provides: "RSC shell with Phase 11-ter placeholder"
      min_lines: 15
  key_links:
    - from: "apps/dashboard/src/app/(app)/layout.tsx"
      to: "apps/dashboard/src/components/chat/ChatBubble.tsx"
      via: "global mount"
      pattern: "ChatBubble"
    - from: "apps/dashboard/tests/e2e/button-audit.spec.ts"
      to: "apps/dashboard/src/lib/button-registry.ts"
      via: "import BUTTON_REGISTRY"
      pattern: "BUTTON_REGISTRY"
---

<objective>
Sweep every interactive surface across the `(app)` route group. For each: confirm it has a working handler with a stable `data-testid`, OR remove it. Catalog the audit in `11-BUTTON-AUDIT.md` and populate `BUTTON_REGISTRY` so the e2e parametric test (Plan 11-08) can click every entry. Mount the ChatBubble globally so it appears on every page. Decide and ship for the Settings page (wire useful or remove). Add `/chat` page shell with the Phase 11-ter placeholder.

Purpose: D-06 (every interactive surface must work or be removed). Currently the dashboard has buttons that do nothing (the Sidebar Chat link is disabled with a "Ships with Phase 4" tooltip; the Settings page is a stub). Phase 11 makes the rule absolute: NO half-implemented buttons.

Output: 1 audit doc, populated registry, global ChatBubble mount, /chat shell, Settings decision, all data-testid additions across affected files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-CONTEXT.md
@.planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-RESEARCH.md
@.planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-PATTERNS.md
@CLAUDE.md
@apps/dashboard/src/app/(app)/layout.tsx
@apps/dashboard/src/components/chat/ChatBubble.tsx
@apps/dashboard/src/components/app-shell/Sidebar.tsx
@apps/dashboard/src/app/(app)/today/Composer.tsx
@apps/dashboard/src/app/(app)/today/TodayView.tsx
@apps/dashboard/src/app/(app)/today/PriorityList.tsx
@apps/dashboard/src/app/(app)/today/DraftsCard.tsx
@apps/dashboard/src/app/(app)/today/DroppedThreads.tsx
@apps/dashboard/src/app/(app)/inbox/InboxClient.tsx
@apps/dashboard/src/app/(app)/inbox/ItemDetail.tsx
@apps/dashboard/src/app/(app)/inbox/ItemRow.tsx
@apps/dashboard/src/app/(app)/entities/page.tsx
@apps/dashboard/src/app/(app)/calendar/CalendarWeekView.tsx
@apps/dashboard/src/app/(app)/settings/page.tsx
@apps/dashboard/src/lib/button-registry.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Sweep + decide + document the button audit</name>
  <read_first>
    - apps/dashboard/src/app/(app)/today/Composer.tsx (verify what it does on submit)
    - apps/dashboard/src/app/(app)/today/PriorityList.tsx (verify row click behavior)
    - apps/dashboard/src/app/(app)/today/DraftsCard.tsx (verify Approve handler)
    - apps/dashboard/src/app/(app)/today/DroppedThreads.tsx (verify click navigates)
    - apps/dashboard/src/app/(app)/inbox/ItemDetail.tsx (verify Save action)
    - apps/dashboard/src/app/(app)/entities/page.tsx (verify filter chips)
    - apps/dashboard/src/app/(app)/calendar/CalendarWeekView.tsx (verify event click)
    - apps/dashboard/src/app/(app)/settings/page.tsx (current stub state)
    - apps/dashboard/src/components/app-shell/Sidebar.tsx (current Chat disabled flag)
    - .planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-RESEARCH.md (Open Q3 — Settings page scope decision)
  </read_first>
  <what-built>
    Manual sweep over every interactive surface in the (app) route group. Operator (Kevin) approves the verdict per surface; planner records each decision in `.planning/phases/11-.../11-BUTTON-AUDIT.md`.
  </what-built>
  <how-to-verify>
    Step 1 — Operator opens https://kos-dashboard-navy.vercel.app and visits each route in the (app) group: /today, /inbox, /entities, /entities/[id], /calendar, /chat (after this plan creates the shell), /integrations-health, /settings. On each route, hover/click every visible interactive surface (button, link, tab, filter, kbd-bound action).

    Step 2 — For each surface, mark in `.planning/phases/11-.../11-BUTTON-AUDIT.md` (create the file using the table below) one of three verdicts:

    | Route | Surface | data-testid (existing or proposed) | Verdict | Action / Handler | Notes |
    |---|---|---|---|---|---|
    | / (sidebar) | T/I/C kbd | sidebar-kbd-T/I/C | KEEP | router.push to /today/inbox/calendar | already wired |
    | / (sidebar) | Chat link | nav-chat | WIRE | href='/chat' (enable, drop disabled flag) | Phase 11-ter shell |
    | / (sidebar) | cmdk palette | sidebar-cmdk | KEEP | opens CommandPalette | already wired |
    | / (sidebar) | Settings link | nav-settings | DECIDE | (a) wire to stub OR (b) hide entry | see Step 3 |
    | / (sidebar) | Logout button | sidebar-logout | KEEP | clears cookie + redirect | already wired |
    | / (sidebar) | Topbar account avatar (if any) | topbar-user-menu | AUDIT | open dropdown with logout | confirm exists |
    | /today | Composer voice button | today-composer-voice | KEEP/AUDIT | should POST to /capture | confirm wired |
    | /today | Composer text input + send | today-composer-send | KEEP/AUDIT | should POST to /capture | confirm wired |
    | /today | StatTile click | (n/a) | KEEP | static — no handler, just visual | no data-testid needed |
    | /today | ChannelHealth row click | mc-channel-bar | KEEP | nav to /integrations-health | added in 11-02 |
    | /today | PriorityList row click | priority-row | AUDIT | should nav to entity page if linked | confirm or remove handler |
    | /today | DraftsCard Approve | drafts-approve | KEEP | server action | already wired |
    | /today | DroppedThreads row click | dropped-row | AUDIT | should nav to /entities/[id] | confirm or remove handler |
    | /today | CapturesList row click | capture-row | DECIDE | nav to source page OR no-op for now | recommend: no-op for v1, add hover-detail later |
    | /inbox | J/K/Enter/E/S kbd | (kbd events) | KEEP | wired in InboxClient | already wired |
    | /inbox | Approve button | inbox-approve-btn | KEEP | server action | wired in 11-03 |
    | /inbox | Edit button | inbox-edit-btn | KEEP | inline editor | wired in 11-03 |
    | /inbox | Skip button | inbox-skip-btn | KEEP | server action | wired in 11-03 |
    | /inbox | ItemRow click | inbox-row-click | KEEP | selects item | already wired |
    | /entities | Filter chips | entity-filter-{type} | KEEP | href links | already wired |
    | /entities/[id] | Edit dialog button | entity-edit-btn | KEEP | opens EditEntityDialog | already wired |
    | /entities/[id]/merge | Merge button | entity-merge-btn | KEEP | merge action | Phase 8 |
    | /calendar | Event click | cal-event-{id} | DECIDE | nav to entity OR no-op | recommend: no-op for v1 |
    | /calendar | Legend | cal-legend | KEEP | static — no handler | not interactive |
    | /chat (NEW) | ChatBubble (global) | chat-bubble | KEEP | opens ChatSheet | added in 11-02 |
    | /integrations-health | Channel link | mc-channel-bar | KEEP | nav to itself (no-op) | confirm |
    | /settings | (decided in Step 3) | (depends) | (depends) | (depends) | |

    Step 3 — Settings decision (D-06 + RESEARCH Open Q3):
    Operator picks one of:
    - (W) Wire stub: keep entry, ship a "Settings — coming with Phase 12: token rotation, channel toggles, manual triggers" placeholder.
    - (R) Remove entry: drop the Sidebar Settings link entirely; Phase 999.x re-introduces.

    Recommendation: (R) Remove. Per D-06 strict rule "NO half-implemented buttons" — a stub Settings page is half-implemented.
    Operator confirms recommended choice or specifies otherwise.

    Step 4 — Operator types "approved" with the Settings verdict (R or W) + any per-row override.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/11-.../11-BUTTON-AUDIT.md` exists with the full audit table populated (no blank Verdict cells)
    - `grep -c "^|" .planning/phases/11-.../11-BUTTON-AUDIT.md` returns >= 25 (header + at least 24 audit rows)
    - Every audit row has a verdict from {KEEP, WIRE, REMOVE, DECIDE} — no row left as "AUDIT" (those must be resolved at this checkpoint)
    - Settings decision recorded explicitly: either "WIRE: stub" or "REMOVE"
    - Operator types "approved" with Settings verdict
  </acceptance_criteria>
  <resume-signal>Type "approved — Settings: REMOVE (or WIRE)" and any per-row overrides.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply audit verdicts — add data-testids, wire missing handlers, remove dead surfaces</name>
  <files>apps/dashboard/src/components/app-shell/Sidebar.tsx, apps/dashboard/src/app/(app)/chat/page.tsx, apps/dashboard/src/app/(app)/settings/page.tsx, apps/dashboard/src/app/(app)/today/Composer.tsx, apps/dashboard/src/app/(app)/today/PriorityList.tsx, apps/dashboard/src/app/(app)/today/DraftsCard.tsx, apps/dashboard/src/app/(app)/today/DroppedThreads.tsx, apps/dashboard/src/app/(app)/inbox/ItemRow.tsx, apps/dashboard/src/app/(app)/entities/page.tsx, apps/dashboard/src/app/(app)/calendar/CalendarWeekView.tsx, apps/dashboard/src/app/(app)/layout.tsx</files>
  <read_first>
    - .planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-BUTTON-AUDIT.md (just completed in Task 1 — read every verdict)
    - apps/dashboard/src/components/app-shell/Sidebar.tsx (Chat NavItem with disabled flag; Settings NavItem)
    - apps/dashboard/src/components/chat/ChatBubble.tsx (already exists from 11-02)
    - apps/dashboard/src/app/(app)/layout.tsx (mount target for ChatBubble)
    - All files in <files> — for each, identify the interactive surfaces flagged in the audit
    - .planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-PATTERNS.md (Workstream E table lines 67-73)
  </read_first>
  <action>
    Step 1 — Mount ChatBubble globally. Edit `apps/dashboard/src/app/(app)/layout.tsx`. Import ChatBubble and place a `<ChatBubble />` element after the existing `<Toaster />` mount inside the layout component's returned JSX.

    Step 2 — Sidebar Chat: flip from disabled. In `apps/dashboard/src/components/app-shell/Sidebar.tsx`, find the NavItem for Chat (currently has `disabled` + `disabledTooltip="Ships with Phase 4"`). Remove both props, set `href="/chat"`, add `data-testid="nav-chat"`.

    Step 3 — Sidebar Settings: apply the audit verdict.
    - If REMOVE: delete the Settings NavItem entirely from Sidebar.tsx.
    - If WIRE (stub): keep the entry, set `data-testid="nav-settings"`, ensure /settings/page.tsx renders an explicit "Settings — coming with Phase 12" placeholder (not a blank stub).

    Step 4 — Create /chat shell at `apps/dashboard/src/app/(app)/chat/page.tsx`:
    ```tsx
    export const dynamic = 'force-dynamic';

    export default function ChatPage() {
      return (
        <div data-testid="chat-page" style={{ padding: 32 }}>
          <h1 style={{ fontSize: 22, fontWeight: 500, marginBottom: 8 }}>Chat with KOS</h1>
          <p style={{ fontSize: 13, color: 'var(--color-text-3)', maxWidth: 600 }}>
            The persistent chat bubble bottom-right opens a quick drawer for ad-hoc questions.
            The full chat interface ships with Phase 11-ter — Sonnet 4.6 + entity-graph context
            + tool-use surface for searching and writing across your KOS data.
          </p>
        </div>
      );
    }
    ```

    Step 5 — Update /settings/page.tsx based on audit verdict:
    - If REMOVE: delete the `apps/dashboard/src/app/(app)/settings/` directory (entire folder + page.tsx)
    - If WIRE (stub): replace existing content with a "coming with Phase 12" placeholder + add `data-testid="settings-page"`

    Step 6 — Add data-testids per audit. For each file in <files>, locate the audited surface and add the corresponding `data-testid` attribute. Examples:
    - Composer.tsx: `data-testid="today-composer-voice"` on voice button, `data-testid="today-composer-send"` on send button
    - PriorityList.tsx: `data-testid="priority-row"` on each row (or REMOVE the onClick if it does nothing — render as div instead of button)
    - DraftsCard.tsx: `data-testid="drafts-approve"` on Approve button
    - DroppedThreads.tsx: `data-testid="dropped-row"` on each row, KEEP nav-to-entity if wired
    - entities/page.tsx: `data-testid={\`entity-filter-\${type}\`}` on filter chips
    - CalendarWeekView.tsx: already has `data-testid="cal-event-${id}"` from 11-05; verify present

    For surfaces marked DECIDE → no-op:
    - PriorityList row click: if it currently has an onClick that does nothing, REMOVE the onClick + change `<button>` wrapper to `<div>`
    - CalendarWeekView event click: same — remove onClick, render as div, hover-tooltip remains
    - CapturesList row click (added in 11-04): leave as div (already has no handler)

    For surfaces marked AUDIT → confirmed wired:
    - today/Composer: confirm onSubmit calls `fetch('/api/capture', ...)` or a server action; if not, wire it OR remove the form. Inspect the current Composer.tsx implementation.

    Step 7 — Run `pnpm -F @kos/dashboard typecheck`.
  </action>
  <verify>
    <automated>pnpm -F @kos/dashboard typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ChatBubble" apps/dashboard/src/app/(app)/layout.tsx | wc -l` returns >= 2 (import + render)
    - `grep "disabled" apps/dashboard/src/components/app-shell/Sidebar.tsx | grep -i "chat\|Phase 4"` returns 0 lines (Chat link is no longer disabled)
    - `grep "data-testid=\"nav-chat\"" apps/dashboard/src/components/app-shell/Sidebar.tsx` returns 1 line
    - File `apps/dashboard/src/app/(app)/chat/page.tsx` exists with `Phase 11-ter` mention
    - Settings verdict applied: either `apps/dashboard/src/app/(app)/settings/` directory exists (WIRE) with `data-testid="settings-page"` OR is gone (REMOVE) — verify with `[ -d apps/dashboard/src/app/(app)/settings ]` matches the audit decision
    - At least 8 new `data-testid=` attributes added across the touched files (audit baseline minimum): `git diff --stat apps/dashboard/src/app/\(app\)/ apps/dashboard/src/components/ | grep "data-testid" || git diff apps/dashboard | grep -c "+.*data-testid="` returns >= 8
    - `pnpm -F @kos/dashboard typecheck` exits 0
  </acceptance_criteria>
  <done>
    - ChatBubble visible on every (app) route
    - No buttons in (app) routes lack handlers
    - All audited surfaces have data-testids
    - Sidebar Chat enabled; Settings decision applied
    - Typecheck clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate BUTTON_REGISTRY from the audit</name>
  <files>apps/dashboard/src/lib/button-registry.ts</files>
  <read_first>
    - .planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-BUTTON-AUDIT.md (the source of truth for the registry)
    - apps/dashboard/src/lib/button-registry.ts (Wave 0 stub — replace with real entries)
    - apps/dashboard/tests/e2e/button-audit.spec.ts (Wave 0 scaffold — confirm BUTTON_REGISTRY import shape)
  </read_first>
  <action>
    Replace the empty stub in `apps/dashboard/src/lib/button-registry.ts` with one entry per surface marked KEEP or WIRE in 11-BUTTON-AUDIT.md (skip REMOVED surfaces — they no longer exist in the DOM):

    ```typescript
    /**
     * Catalog of every interactive surface in the (app) route group.
     * Source: .planning/phases/11-.../11-BUTTON-AUDIT.md (KEEP + WIRE entries).
     * Used by tests/e2e/button-audit.spec.ts for parametric click verification.
     *
     * Each entry: a stable data-testid + the route the test should visit + the
     * expected behaviour after click ('navigate' | 'request' | 'kbd' | 'open').
     */
    export const BUTTON_REGISTRY: ReadonlyArray<{
      id: string;
      route: string;
      expect: 'navigate' | 'request' | 'kbd' | 'open' | 'select';
    }> = [
      // Sidebar (visible on every route — pick /today as the reference)
      { id: 'nav-today', route: '/today', expect: 'navigate' },
      { id: 'nav-inbox', route: '/today', expect: 'navigate' },
      { id: 'nav-entities', route: '/today', expect: 'navigate' },
      { id: 'nav-calendar', route: '/today', expect: 'navigate' },
      { id: 'nav-chat', route: '/today', expect: 'navigate' },
      { id: 'nav-integrations-health', route: '/today', expect: 'navigate' },
      { id: 'sidebar-logout', route: '/today', expect: 'navigate' },

      // Chat bubble (global)
      { id: 'chat-bubble', route: '/today', expect: 'open' },

      // /today
      { id: 'today-composer-send', route: '/today', expect: 'request' },
      { id: 'drafts-approve', route: '/today', expect: 'request' },

      // /inbox
      { id: 'inbox-approve-btn', route: '/inbox', expect: 'request' },
      { id: 'inbox-skip-btn', route: '/inbox', expect: 'request' },
      { id: 'inbox-edit-btn', route: '/inbox', expect: 'open' },

      // /entities
      { id: 'entity-edit-btn', route: '/entities', expect: 'open' },

      // (Settings entry conditional on audit verdict — comment out if removed)
      // { id: 'nav-settings', route: '/today', expect: 'navigate' },
    ];
    ```

    Adapt the array to the exact verdicts in 11-BUTTON-AUDIT.md. If Settings was REMOVED, comment out or omit the nav-settings entry. If a row had verdict DECIDE → no-op, do NOT add to registry (it's not a button anymore).

    Step 2 — Verify the e2e parametric test imports work: `pnpm -F @kos/dashboard typecheck` + open `apps/dashboard/tests/e2e/button-audit.spec.ts` and confirm the BUTTON_REGISTRY import path is `'@/lib/button-registry'`. The Wave 0 scaffold should already have this; if not, fix.
  </action>
  <verify>
    <automated>pnpm -F @kos/dashboard typecheck && grep -c "id: " apps/dashboard/src/lib/button-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/src/lib/button-registry.ts` exports BUTTON_REGISTRY as a non-empty array (`grep -c "id: " apps/dashboard/src/lib/button-registry.ts` returns >= 10)
    - Every `id:` value in the registry is a real `data-testid` present in some component file: `for id in $(grep -oP "id: '[^']+'" apps/dashboard/src/lib/button-registry.ts | grep -oP "'[^']+'" | tr -d "'"); do grep -rq "data-testid=\"$id\"" apps/dashboard/src/ || echo "MISSING: $id"; done` returns 0 MISSING lines
    - `pnpm -F @kos/dashboard typecheck` exits 0
    - `apps/dashboard/tests/e2e/button-audit.spec.ts` imports `BUTTON_REGISTRY` (still skipped — Plan 11-08 activates)
  </acceptance_criteria>
  <done>
    - BUTTON_REGISTRY populated with every KEEP/WIRE surface from the audit
    - Every registry id corresponds to a real data-testid in the DOM
    - Typecheck clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → DOM | data-testid attributes leak component structure to anyone with DevTools | accept |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | I (Information disclosure) | data-testid attributes are visible in production HTML | accept | Single-user app; the testids reveal nothing not already visible. Removing them in prod requires a build-time strip step that adds tooling cost without security gain. |
| T-11-07-02 | T (Tampering) | A removed button might still have a stale handler in app code | mitigate | Audit task explicitly removes both the rendered element AND its onClick. typecheck catches imports that became unused. |
| T-11-07-03 | E | Settings stub becomes a vector if backend later treats /settings as authenticated-but-unverified | accept | /settings is just a static placeholder; no /api/settings handler exists. Phase 12 will add proper auth + verification when backend is built. |
</threat_model>

<verification>
- `pnpm -F @kos/dashboard typecheck` exits 0
- `pnpm -F @kos/dashboard test --run` exits 0 (no regressions)
- Manual: visit every (app) route on the live dashboard; every visible button either fires a request or navigates
- BUTTON_REGISTRY length matches the count of KEEP+WIRE rows in the audit
</verification>

<success_criteria>
- D-06 satisfied: every interactive surface either works or has been removed
- Audit document committed as the source-of-truth for the e2e parametric test
- ChatBubble globally visible
- Settings decision shipped (REMOVE or WIRE: stub)
- All KEEP/WIRE surfaces have stable data-testids
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-rebuild-real-data-wiring-button-audit-mission-contr/11-07-SUMMARY.md` with:
1. Audit row counts: KEEP / WIRE / REMOVE breakdown
2. Settings verdict applied
3. BUTTON_REGISTRY length
4. Files modified + LOC delta
5. Note any surfaces that needed actual handler wiring (not just data-testid additions)
</output>
</content>
</invoke>