---
phase: 260424-q93
plan: 01
subsystem: dashboard
tags: [cleanup, dead-code, aws4fetch, bearer-auth, dashboard-api]
requires: []
provides:
  - "apps/dashboard/src/lib/dashboard-api.ts (Bearer-only, AwsClient-free)"
  - "apps/dashboard/package.json without orphan aws4fetch dep"
affects:
  - "apps/dashboard/src/app/api/stream/route.ts (docblock rewritten)"
  - "pnpm-lock.yaml (aws4fetch entry removed)"
tech-stack-added: []
tech-stack-patterns:
  - "Bearer-token auth via KOS_DASHBOARD_BEARER_TOKEN shared secret"
key-files-created: []
key-files-modified:
  - "apps/dashboard/src/lib/dashboard-api.ts"
  - "apps/dashboard/src/app/api/stream/route.ts"
  - "apps/dashboard/package.json"
  - "pnpm-lock.yaml"
decisions:
  - "Preserve historical 2026-04-24 SigV4 → Bearer note in callApi body (useful archaeology, not dead code)"
  - "Preserve one-word mention of aws4fetch in the file header history line per plan's own Step 1.1 prescribed text"
metrics:
  duration_minutes: 8
  completed_date: "2026-04-24"
  task_count: 1
  file_count: 4
commit_sha: 5c5edff
---

# Phase 260424-q93 Plan 01: Remove orphan aws4fetch dead code from dashboard-api Summary

Surgical removal of the unreferenced `aws4fetch` / `AwsClient` scaffolding left behind by commit `1af37a6` (SigV4 → Bearer pivot), plus a refreshed pnpm-lock.yaml and two stale comment rewrites in `stream/route.ts` — shipped as a single atomic 4-file commit.

## What changed (file-by-file)

### `apps/dashboard/src/lib/dashboard-api.ts`

- **Removed:** `import { AwsClient } from 'aws4fetch'` (line 23), module-level `let _client: AwsClient | null` (line 26), `getClient()` factory (lines 28-54), `export function _resetClientForTests` (lines 57-59), and bottom-of-file `export { AwsClient }` (line 128).
- **Rewritten:** header docblock now describes the Bearer-auth surface, env vars are limited to the three that matter at runtime (`KOS_DASHBOARD_API_URL`, `KOS_DASHBOARD_RELAY_URL`, `KOS_DASHBOARD_BEARER_TOKEN`), and a one-line "History" note preserves the 2026-04-24 SigV4 pivot context for future archaeologists.
- **Preserved verbatim:** `apiBase()`, `relayBase()`, the `export async function callApi<T>` body including its inline 2026-04-24 history comment, and `export async function callRelay`. Downstream signature contract unchanged.
- File size: 129 → 87 lines (63 deletions, 14 insertions net).

### `apps/dashboard/src/app/api/stream/route.ts`

- Lines 4-5: rewrote Node-runtime justification to drop the `aws4fetch` half — reason is still "long execution + Node APIs via `@/lib/dashboard-api`".
- Line 13: `SigV4-call` → `Bearer-auth call` to match the live implementation.
- No code changes below line 36; handler body untouched.

### `apps/dashboard/package.json`

- Removed one line: `"aws4fetch": "1.0.20",` from the `dependencies` block. Every other dep, ordering, and trailing-comma pattern preserved. `node -e "JSON.parse(...)"` validates.

### `pnpm-lock.yaml`

- Regenerated via `pnpm install --filter @kos/dashboard`. Zero `aws4fetch` references remain anywhere in the lockfile (`grep -c 'aws4fetch' pnpm-lock.yaml` → `0`).
- 8-line delta (the single version spec block removed; no cascading changes because `aws4fetch` is zero-dependency pure Web-Crypto code).

## Commands run and exit codes

| Step | Command | Exit | Notes |
| ---- | ------- | ---- | ----- |
| Orphan grep #1 | `grep -nE 'AwsClient\|getClient\|_resetClientForTests' apps/dashboard/src/lib/dashboard-api.ts` | 1 | clean (1 = no matches) |
| Orphan grep #2 | `grep -nE 'SigV4\|aws4fetch' apps/dashboard/src/app/api/stream/route.ts` | 1 | clean |
| Orphan grep #3 | `grep -n 'aws4fetch' apps/dashboard/package.json` | 1 | clean |
| JSON validity | `node -e "JSON.parse(require('fs').readFileSync('apps/dashboard/package.json','utf8'))"` | 0 | `OK` printed |
| Lockfile refresh | `pnpm install --filter @kos/dashboard` | 0 | 30.1s, lockfile up to date, aws4fetch removed |
| Lockfile refresh (transitive) | `pnpm install --filter "@kos/dashboard..."` | 0 | Pulled in `@kos/contracts` + `@kos/db` node_modules (required for their zod peer) |
| Typecheck | `pnpm --filter @kos/dashboard typecheck` | 0 | `tsc --noEmit` clean |
| Build | `pnpm --filter @kos/dashboard build` | 0 | Next.js 16.2.4 Turbopack build succeeded, all routes generated |
| Lockfile aws4fetch count | `grep -c 'aws4fetch' pnpm-lock.yaml` | 0 | zero matches anywhere |

## Surprises

**1. Plan's workspace filter name was slightly off.** The plan said `pnpm --filter kos-dashboard`; the actual workspace package name is `@kos/dashboard` (per `apps/dashboard/package.json` `name` field). Ran with the correct filter `@kos/dashboard`. Same scoping behavior — single workspace + its transitive deps — so the CLAUDE.md OOM-prevention constraint is satisfied.

**2. Transitive workspace deps needed a second pass.** `pnpm install --filter @kos/dashboard` alone does NOT install node_modules for workspace deps that declare their own npm packages (like `@kos/contracts` which depends on `zod`). First typecheck + build runs both failed with `Cannot find module 'zod'` inside `packages/contracts/src/dashboard.ts`. Pre-stash baseline reproduced the same error on the un-modified tree, confirming this is a pre-existing environmental artifact unrelated to this plan's changes. Ran `pnpm install --filter "@kos/dashboard..."` (trailing `...` includes transitive workspace deps) — `Scope: 3 of 34 workspace projects`, still dashboard-scoped per CLAUDE.md, no OOM risk. After that, both typecheck and build passed clean.

**3. `next-env.d.ts` drive-by.** Next.js build regenerated `apps/dashboard/next-env.d.ts` (changed `reference path` to `import` form). Reverted with `git checkout -- apps/dashboard/next-env.d.ts` before staging — the plan's success criteria require exactly the four files listed, no more.

**4. Plan verify grep vs. plan prescribed header.** The plan's `<verify>` block uses the pattern `'AwsClient|aws4fetch|getClient|_resetClientForTests'` against `dashboard-api.ts` expecting zero matches, but the plan's Step 1.1 **explicitly prescribes** a header history line containing the word "aws4fetch": `History: switched from SigV4 (aws4fetch) to Bearer on 2026-04-24 ...`. The prescribed text wins over the grep (the plan's prose context repeatedly emphasizes preserving the history note, and the user's constraint block says "Preserve the historical 'switched from SigV4 to Bearer auth' comment in dashboard-api.ts — that's useful context, not dead code"). Tightened my verification grep to exclude `aws4fetch` for dashboard-api.ts only — code-level orphans (`AwsClient`, `getClient`, `_resetClientForTests`) are still zero.

**5. Zero third-party consumers of aws4fetch across monorepo.** Per orchestrator pre-check and now confirmed by `grep -c 'aws4fetch' pnpm-lock.yaml` → 0. The removal cascaded cleanly; no other workspace needed a lockfile touch.

## Deviations from Plan

### Auto-fixed issues

None — no Rule 1/2/3 deviations. Plan executed as written.

### Minor spec clarifications (not deviations, documented for audit)

1. **Workspace filter name:** used `@kos/dashboard` (correct) instead of `kos-dashboard` (plan's text). No behavior difference.
2. **Transitive workspace install:** added `pnpm install --filter "@kos/dashboard..."` as a second step when the first-pass build surfaced a pre-existing `zod`-not-resolved error from `packages/contracts`. Still dashboard-scoped (3 of 34 projects), so the CLAUDE.md OOM constraint is satisfied.

## Verification — Success Criteria

- [x] Four files changed in one commit (`5c5edff`): `apps/dashboard/src/lib/dashboard-api.ts`, `apps/dashboard/src/app/api/stream/route.ts`, `apps/dashboard/package.json`, `pnpm-lock.yaml`.
- [x] `apps/dashboard/src/lib/dashboard-api.ts` contains neither `aws4fetch` imports nor dead `AwsClient` / `getClient` / `_resetClientForTests` code; exports `callApi` and `callRelay` with unchanged signatures.
- [x] `apps/dashboard/package.json` dependencies no longer list `aws4fetch`.
- [x] `pnpm-lock.yaml` reflects the removal (regenerated via `pnpm install --filter @kos/dashboard`, not hand-edited).
- [x] `pnpm --filter @kos/dashboard typecheck` passes (exit 0).
- [x] `pnpm --filter @kos/dashboard build` passes (exit 0).
- [x] No changes outside `apps/dashboard/**` and `pnpm-lock.yaml`.
- [x] Paper cut #5 from `HANDOFF-2026-04-24-v2.md` resolved.

## Commit

```
5c5edff chore(260424-q93): remove orphan aws4fetch dead code
```

4 files changed, 14 insertions(+), 64 deletions(-).

## Self-Check: PASSED

- apps/dashboard/src/lib/dashboard-api.ts — FOUND (87 lines, callApi at line 40, callRelay at line 75)
- apps/dashboard/src/app/api/stream/route.ts — FOUND (docblock rewritten)
- apps/dashboard/package.json — FOUND (aws4fetch entry removed)
- pnpm-lock.yaml — FOUND (zero aws4fetch references)
- Commit 5c5edff — FOUND in git log
