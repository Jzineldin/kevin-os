---
phase: 260424-q93
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/lib/dashboard-api.ts
  - apps/dashboard/src/app/api/stream/route.ts
  - apps/dashboard/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - QUICK-q93-01
  - QUICK-q93-02

must_haves:
  truths:
    - "`apps/dashboard/src/lib/dashboard-api.ts` contains zero references to `AwsClient`, `aws4fetch`, `getClient`, `_resetClientForTests`, or `_client`."
    - "`apps/dashboard/src/lib/dashboard-api.ts` still exports `callApi` and `callRelay` with the exact same runtime signatures the 20 downstream files already import."
    - "The file-level docblock and inline comments in `dashboard-api.ts` describe Bearer-token auth (not SigV4)."
    - "The docblock in `apps/dashboard/src/app/api/stream/route.ts` no longer claims `aws4fetch` as the reason for the Node runtime pin."
    - "`aws4fetch` is absent from `apps/dashboard/package.json` dependencies."
    - "`pnpm-lock.yaml` has `aws4fetch` removed from the `@kos/dashboard` importer and root packages map (unless another workspace package still pulls it — none do, per orchestrator pre-check)."
    - "`pnpm --filter kos-dashboard typecheck` exits 0."
    - "`pnpm --filter kos-dashboard build` exits 0."
  artifacts:
    - path: "apps/dashboard/src/lib/dashboard-api.ts"
      provides: "Bearer-auth `callApi` and `callRelay` helpers, nothing else."
      contains: "export async function callApi"
    - path: "apps/dashboard/package.json"
      provides: "Dashboard workspace manifest without orphan `aws4fetch` dep."
      contains: "\"@kos/dashboard\""
  key_links:
    - from: "apps/dashboard/src/lib/dashboard-api.ts"
      to: "20 downstream importers of callApi/callRelay"
      via: "named exports `callApi` and `callRelay`"
      pattern: "export async function (callApi|callRelay)"
    - from: "apps/dashboard/package.json"
      to: "pnpm-lock.yaml"
      via: "`pnpm install --filter kos-dashboard` regenerates lockfile entry"
      pattern: "aws4fetch"
---

<objective>
Remove orphan `aws4fetch` dead code from the dashboard app left behind by commit `1af37a6` (the SigV4 → Bearer auth pivot). The `AwsClient` import, memoized `_client` / `getClient` helper, `_resetClientForTests` export, bottom-of-file `AwsClient` re-export, and the `aws4fetch` package.json dependency are all unreferenced. Stale comments in `dashboard-api.ts` and `stream/route.ts` still describe SigV4 and must be rewritten to match the live Bearer-auth implementation.

Purpose: eliminate paper cut #5 from the 2026-04-24 handoff — misleading comments that lie about the live auth path, plus a ~1 KB cold-start dep that no code calls. Future Claude sessions will stop wasting context trying to reconcile "why does this file import aws4fetch if we use Bearer?".

Output: smaller `dashboard-api.ts` (drop lines 23, 26-59, 128 plus comment rewrites), accurate `stream/route.ts` docblock, `aws4fetch` removed from dashboard `package.json`, `pnpm-lock.yaml` refreshed via `pnpm install --filter kos-dashboard`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/HANDOFF-2026-04-24-v2.md
@apps/dashboard/src/lib/dashboard-api.ts
@apps/dashboard/src/app/api/stream/route.ts
@apps/dashboard/package.json

<interfaces>
<!-- Current exports of apps/dashboard/src/lib/dashboard-api.ts (only these must survive). -->

```typescript
// LIVE — 20 downstream files depend on these. Do not touch signatures.
export async function callApi<T>(
  path: string,
  init: RequestInit,
  schema: z.ZodSchema<T>,
): Promise<T>;

export async function callRelay(
  path: string,
  init: RequestInit,
): Promise<Response>;
```

<!-- DEAD — remove entirely. Zero importers confirmed by orchestrator pre-check
     (grep across apps/dashboard/src/ returned zero matches for AwsClient /
     getClient / _resetClientForTests / _client outside dashboard-api.ts itself). -->

```typescript
import { AwsClient } from 'aws4fetch';            // line 23 — DELETE
let _client: AwsClient | null = null;              // line 26 — DELETE
function getClient(): AwsClient { ... }            // lines 28-54 — DELETE
export function _resetClientForTests(): void { ... } // lines 57-59 — DELETE
export { AwsClient };                              // line 128 — DELETE
```

<!-- Comments to rewrite (not delete — keep a short accurate header). -->

- `dashboard-api.ts` lines 1-22: header docblock references `SigV4 client`, `AwsClient instance`, `AWS_ACCESS_KEY_ID_DASHBOARD`, and "Why aws4fetch not @aws-sdk/signature-v4". Rewrite to describe the Bearer-auth surface.
- `dashboard-api.ts` line 84-88 inline comment: keep the "2026-04-24: switched from SigV4 to Bearer auth" history note — it's useful archaeology. Remove only the dead code above, not the explanatory comment inside `callApi`.
- `stream/route.ts` lines 4-5: "Runtime: Node (P-01 forbids Edge here — aws4fetch + long execution need Node APIs...)". Rewrite: drop the `aws4fetch` half — the reason is still valid (long execution / Node APIs in `callRelay`) but `aws4fetch` is no longer part of it.
- `stream/route.ts` line 13: "SigV4-call `callRelay(...)`" — rewrite to "Bearer-auth call" to match the live implementation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete aws4fetch dead code + refresh lockfile in one atomic change</name>
  <files>
    apps/dashboard/src/lib/dashboard-api.ts,
    apps/dashboard/src/app/api/stream/route.ts,
    apps/dashboard/package.json,
    pnpm-lock.yaml
  </files>
  <action>
**Step 1 — Edit `apps/dashboard/src/lib/dashboard-api.ts`:**

Rewrite the file so the final shape is: (a) a short accurate header docblock, (b) `import type { z } from 'zod';`, (c) `apiBase()` helper, (d) `relayBase()` helper, (e) exported `callApi`, (f) exported `callRelay`. Nothing else.

Specific edits:

1. Replace the header docblock (current lines 1-22) with a concise Bearer-auth description. Suggested replacement:

```typescript
/**
 * Server-side Bearer-auth clients for `services/dashboard-api` (Lambda
 * Function URL) and `services/dashboard-listen-relay`. One shared secret
 * (`KOS_DASHBOARD_BEARER_TOKEN`) gates both — the same token Vercel
 * middleware already uses to protect /login.
 *
 * Public surface:
 *   - callApi<T>(path, init, schema): zod-validated JSON response.
 *   - callRelay(path, init): raw Response for streaming / long-poll.
 *
 * Required env vars at runtime:
 *   - KOS_DASHBOARD_API_URL       (Lambda Function URL base)
 *   - KOS_DASHBOARD_RELAY_URL     (relay Function URL base, used by SSE)
 *   - KOS_DASHBOARD_BEARER_TOKEN  (shared secret, set on Vercel)
 *
 * History: switched from SigV4 (aws4fetch) to Bearer on 2026-04-24 after
 * the kos-dashboard-caller IAM user hit undebugable 403s against the
 * Function URL despite matching identity + resource policies.
 */
```

2. Delete line 23: `import { AwsClient } from 'aws4fetch';`
3. Keep line 24: `import type { z } from 'zod';`
4. Delete lines 26-59 entirely (the `_client` module-level, `getClient()` function, and `_resetClientForTests` export).
5. Keep `apiBase()` and `relayBase()` unchanged.
6. Keep the `export async function callApi` body unchanged — including its inline comment at the current lines 84-88 about the 2026-04-24 switch. That history note is the only remaining mention of SigV4 inside the file body and it belongs there.
7. Keep the `export async function callRelay` body unchanged.
8. Delete the final line: `export { AwsClient };`

After the edit, the file should be approximately 65-70 lines. Run `grep -nE 'AwsClient|aws4fetch|getClient|_resetClientForTests|_client' apps/dashboard/src/lib/dashboard-api.ts` — expected output: empty.

**Step 2 — Edit `apps/dashboard/src/app/api/stream/route.ts`:**

Two comment-only edits inside the top docblock. Do NOT touch any code below line 36.

1. Lines 4-5 currently read:
   ```
    * Runtime: Node (P-01 forbids Edge here — aws4fetch + long execution need
    * Node APIs via `@/lib/dashboard-api`).
   ```
   Replace with:
   ```
    * Runtime: Node (P-01 forbids Edge here — long execution + Node APIs
    * via `@/lib/dashboard-api` keep this pinned to the Node runtime).
   ```

2. Line 13 currently contains: `*   - Loop: SigV4-call \`callRelay('/events?cursor=<last>&wait=25')\`. For`
   Replace with: `*   - Loop: Bearer-auth call \`callRelay('/events?cursor=<last>&wait=25')\`. For`

After the edit, run `grep -nE 'SigV4|aws4fetch' apps/dashboard/src/app/api/stream/route.ts` — expected output: empty.

**Step 3 — Edit `apps/dashboard/package.json`:**

Remove the single line `"aws4fetch": "1.0.20",` from the `dependencies` object (currently line 27). Do not touch any other entry, do not reorder, do not alter trailing commas on neighbor lines (the line above it — `"@vercel/speed-insights": "2.0.0",` — already has its trailing comma intact, and `"class-variance-authority"` below keeps its own comma pattern; only the middle line disappears).

After the edit, run `grep -n 'aws4fetch' apps/dashboard/package.json` — expected output: empty. Then run `cd apps/dashboard && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` to confirm the file is still valid JSON.

**Step 4 — Refresh the lockfile (DASHBOARD-SCOPED):**

Run exactly this command from the repo root: `pnpm install --filter kos-dashboard`

CRITICAL: do NOT run `pnpm install` without `--filter`. Per HANDOFF-2026-04-24-v2.md, a monorepo-wide install OOM'd the EC2 earlier today. The `--filter kos-dashboard` scope keeps the resolver bounded to this one workspace plus its transitive graph.

Expected behavior: pnpm resolves the workspace, notices `aws4fetch` is no longer requested, updates `pnpm-lock.yaml`, and (probably) prints a short "-1 package removed" summary. If pnpm reports that `aws4fetch` is still referenced by another importer, STOP and report it — the orchestrator pre-check said it was orphan-only to the dashboard, so any other importer would be news.

**Step 5 — Verification (dashboard-scoped only):**

Run `pnpm --filter kos-dashboard typecheck` — expect exit 0. This will catch any case where deleting `AwsClient` broke a type reference we missed.

Run `pnpm --filter kos-dashboard build` — expect exit 0. Next.js build will fail loudly if the `stream/route.ts` module stopped compiling.

Do NOT run `pnpm test` across the monorepo. If you want to run dashboard tests locally, scope them: `pnpm --filter kos-dashboard test`. Optional, not required for this plan.

**Step 6 — Commit as one atomic change:**

Stage the four touched files and commit with a message like:

```
chore(dashboard): remove orphan aws4fetch dead code

- Drop AwsClient import, memoized client, getClient(), and bottom-of-file
  re-export from dashboard-api.ts — zero downstream consumers after the
  2026-04-24 SigV4 → Bearer pivot (1af37a6).
- Drop aws4fetch dep from apps/dashboard/package.json.
- Rewrite stale SigV4/aws4fetch comments in dashboard-api.ts and
  stream/route.ts to describe the live Bearer-auth path.
- Refresh pnpm-lock.yaml via `pnpm install --filter kos-dashboard`.
```

Do not amend any prior commit; create a new one. Do not push; the user will handle that.
  </action>
  <verify>
    <automated>
      grep -nE 'AwsClient|aws4fetch|getClient|_resetClientForTests' apps/dashboard/src/lib/dashboard-api.ts ; [ $? -ne 0 ] &amp;&amp; \
      grep -nE 'SigV4|aws4fetch' apps/dashboard/src/app/api/stream/route.ts ; [ $? -ne 0 ] &amp;&amp; \
      grep -n 'aws4fetch' apps/dashboard/package.json ; [ $? -ne 0 ] &amp;&amp; \
      pnpm --filter kos-dashboard typecheck &amp;&amp; \
      pnpm --filter kos-dashboard build
    </automated>
  </verify>
  <done>
    - `grep -nE 'AwsClient|aws4fetch|getClient|_resetClientForTests' apps/dashboard/src/lib/dashboard-api.ts` returns no matches.
    - `grep -nE 'SigV4|aws4fetch' apps/dashboard/src/app/api/stream/route.ts` returns no matches (header docblock rewritten, body comment at line 84 of dashboard-api.ts may still reference "SigV4" since the history note lives there — that's intentional and NOT in scope for this grep; the grep above is scoped to stream/route.ts, not dashboard-api.ts).
    - `grep -n 'aws4fetch' apps/dashboard/package.json` returns no matches.
    - `pnpm --filter kos-dashboard typecheck` exits 0.
    - `pnpm --filter kos-dashboard build` exits 0.
    - `git diff --stat HEAD~1` shows exactly four files changed: `apps/dashboard/src/lib/dashboard-api.ts`, `apps/dashboard/src/app/api/stream/route.ts`, `apps/dashboard/package.json`, `pnpm-lock.yaml`.
    - `callApi` and `callRelay` named exports survive and keep identical signatures (downstream 20 importers unaffected).
  </done>
</task>

</tasks>

<verification>
Single-task plan, so plan-level verification matches task-level. Additional spot-check: open any one downstream consumer (e.g., `grep -l "from '@/lib/dashboard-api'" apps/dashboard/src | head -1`), confirm it still imports `callApi` or `callRelay` and not the deleted `AwsClient` / `_resetClientForTests` names. The orchestrator pre-check already confirmed zero consumers of the dead exports, so this is belt-and-braces.
</verification>

<success_criteria>
- Four files changed in one commit: `apps/dashboard/src/lib/dashboard-api.ts`, `apps/dashboard/src/app/api/stream/route.ts`, `apps/dashboard/package.json`, `pnpm-lock.yaml`.
- `apps/dashboard/src/lib/dashboard-api.ts` contains neither `aws4fetch` imports nor dead `AwsClient` / `getClient` / `_resetClientForTests` code; exports `callApi` and `callRelay` with unchanged signatures.
- `apps/dashboard/package.json` dependencies no longer list `aws4fetch`.
- `pnpm-lock.yaml` reflects the removal (regenerated via `pnpm install --filter kos-dashboard`, NOT hand-edited, NOT via unscoped `pnpm install`).
- `pnpm --filter kos-dashboard typecheck` passes.
- `pnpm --filter kos-dashboard build` passes.
- No changes outside `apps/dashboard/**` and `pnpm-lock.yaml`.
- Paper cut #5 from HANDOFF-2026-04-24-v2.md is resolved.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-q93-remove-orphan-aws4fetch-dead-code-from-d/260424-q93-01-SUMMARY.md` with:
- What changed (file-by-file diff summary)
- Commands run (pnpm install scope, typecheck, build) and their exit codes
- Any surprises (e.g., if pnpm reported additional workspaces still depended on aws4fetch — shouldn't happen, but note if it does)
- Commit SHA of the atomic change
</output>
