import { test } from './fixtures';

// Maps to UI-03 (03-VALIDATION.md Wave 0).
// Wave 0 stub — wired in plan 03-10 (Calendar Deadline/Idag view).
test.describe('calendar', () => {
  test.fixme('renders Command Center Deadline rows bolag-coloured', async () => {
    // GET /calendar, assert event blocks carry data-bolag attributes with
    // the correct colour tokens.
  });
});
