import { test } from './fixtures';

// Maps to UI-01 (03-VALIDATION.md Wave 0).
// Wave 0 stub — wired in plan 03-04 (Today view composition).
test.describe('today', () => {
  test.fixme('renders brief, Top 3, drafts, dropped threads, meetings', async () => {
    // GET /, expect five sections with deterministic fixture data.
  });
});
