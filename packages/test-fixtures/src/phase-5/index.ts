/**
 * Phase 5 Plan 05-00 — barrel re-export for messaging-channels fixtures.
 * Imported via the package root barrel (`@kos/test-fixtures`) and via the
 * scoped subpath (`@kos/test-fixtures/phase-5` if the package adds an
 * exports entry in a later plan).
 */
export * from './mv3-runtime-stub.js';
export * from './voyager-response.js';
export * from './baileys-incoming.js';
