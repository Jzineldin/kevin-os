/**
 * Phase 5 Plan 05-00 scaffold — placeholder test. Real handler tests land in
 * Plan 05-02 once the body is implemented. This file exists so vitest
 * resolves the workspace and `pnpm -r test` doesn't trip over a missing
 * test directory.
 */
import { describe, it } from 'vitest';

describe.skip('chrome-webhook handler (scaffold placeholder)', () => {
  it('placeholder — body lands in Plan 05-02', () => {
    /* intentionally empty */
  });
});
