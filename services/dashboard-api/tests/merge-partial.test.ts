import { describe, it } from 'vitest';

// Wave 0 stub — wired in plan 03-08 (partial failure path).
describe('merge partial failure', () => {
  it.todo('Notion archive fails after RDS commit -> partial_merge_state row written');
  it.todo('surfaces Resume? InboxItem keyed to merge_id');
});
