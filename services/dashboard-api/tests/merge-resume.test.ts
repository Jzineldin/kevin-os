import { describe, it } from 'vitest';

// Wave 0 stub — wired in plan 03-08 (Resume endpoint).
describe('merge resume', () => {
  it.todo('POST /entities/:id/merge/resume with merge_id re-drives archive step');
  it.todo('marks partial_merge_state as completed and removes Resume? InboxItem');
});
