/**
 * @kos/service-telegram-bot — scaffold.
 * Real implementation lands in Plan 02-03 (CAP-01 Telegram ingress).
 */
export const handler = async (event: unknown): Promise<{ ok: true }> => {
  void event;
  return { ok: true };
};
