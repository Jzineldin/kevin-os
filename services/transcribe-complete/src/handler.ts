/**
 * @kos/service-transcribe-complete — scaffold.
 * Real implementation lands in a later Phase 2 plan.
 */
export const handler = async (event: unknown): Promise<{ ok: true }> => {
  void event;
  return { ok: true };
};
